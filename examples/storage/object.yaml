apiVersion: storage.yandex-cloud.jet.crossplane.io/v1alpha1
kind: Object
metadata:
  name: provider-jet-yc-object
spec:
  forProvider:
    contentBase64: MTIzNDU2Nzg=
    key: "cute-cat"
    serviceAccountKeyRef:
      name: provider-jet-sa-key
    secretKeySecretRef:
      name: s3-sa-key-conn
      namespace: crossplane-system
      key: attribute.private_key
    bucketRef:
      name: provider-jet-yc-bucket
  providerConfigRef:
    name: default
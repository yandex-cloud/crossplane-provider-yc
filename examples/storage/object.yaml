apiVersion: storage.yandex-cloud.jet.crossplane.io/v1alpha1
kind: Object
metadata:
  name: provider-jet-yc-object
spec:
  forProvider:
    contentBase64: MTIzNDU2Nzg=
    key: "cute-cat"
    accessKeyRef:
      name: provider-jet-yc-sa-static-key
    secretKeySecretRef:
      name: sa-key-conn
      namespace: crossplane-system
      key: attribute.secret_key
    bucketRef:
      name: provider-jet-yc-bucket
  providerConfigRef:
    name: default
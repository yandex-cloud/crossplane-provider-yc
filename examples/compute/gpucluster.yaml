apiVersion: compute.yandex-cloud.jet.crossplane.io/v1alpha1
kind: GpuCluster
metadata:
  name: example-gpu-cluster
spec:
  forProvider:
    name: crossplane-provider-yc-e2e-gpu-cluster
    description: crossplane-provider-yc E2E GPU cluster
    zone: ru-central1-a
  providerConfigRef:
    name: default

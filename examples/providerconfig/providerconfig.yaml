apiVersion: yandex-cloud.jet.crossplane.io/v1alpha1
kind: ProviderConfig
metadata:
  name: example
spec:
  credentials:
    cloudId: ${cloud_id}
    folderId: ${folder_id}
    source: Secret
    secretRef:
      name: yc-creds
      namespace: crossplane-system
      key: credentials
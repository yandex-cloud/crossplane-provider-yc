---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.11.4
  name: mysqlclusters.mdb.yandex-cloud.jet.crossplane.io
spec:
  group: mdb.yandex-cloud.jet.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - yandex-cloud
    kind: MySQLCluster
    listKind: MySQLClusterList
    plural: mysqlclusters
    singular: mysqlcluster
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: MySQLCluster is the Schema for the MySQLClusters API. Manages
          a MySQL cluster within Yandex.Cloud.
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: MySQLClusterSpec defines the desired state of MySQLCluster
            properties:
              deletionPolicy:
                default: Delete
                description: 'DeletionPolicy specifies what will happen to the underlying
                  external when this managed resource is deleted - either "Delete"
                  or "Orphan" the external resource. This field is planned to be deprecated
                  in favor of the ManagementPolicy field in a future release. Currently,
                  both could be set independently and non-default values would be
                  honored if the feature flag is enabled. See the design doc for more
                  information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223'
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  access:
                    description: Access policy to the MySQL cluster. The structure
                      is documented below.
                    items:
                      properties:
                        dataLens:
                          description: Allow access for Yandex DataLens.
                          type: boolean
                        dataTransfer:
                          description: Allow access for DataTransfer
                          type: boolean
                        webSql:
                          description: Allows access for SQL queries in the management
                            console.
                          type: boolean
                      type: object
                    type: array
                  allowRegenerationHost:
                    description: A host of the MySQL cluster. The structure is documented
                      below.
                    type: boolean
                  backupRetainPeriodDays:
                    description: The period in days during which backups are stored.
                    type: number
                  backupWindowStart:
                    description: Time to start the daily backup, in the UTC. The structure
                      is documented below.
                    items:
                      properties:
                        hours:
                          description: The hour at which backup will be started.
                          type: number
                        minutes:
                          description: The minute at which backup will be started.
                          type: number
                      type: object
                    type: array
                  database:
                    description: (Deprecated) To manage databases, please switch to
                      using a separate resource type yandex_mdb_mysql_databases.
                    items:
                      properties:
                        name:
                          description: The name of the database.
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                  deletionProtection:
                    description: Inhibits deletion of the cluster.  Can be either
                      true or false.
                    type: boolean
                  description:
                    description: Description of the MySQL cluster.
                    type: string
                  environment:
                    description: Deployment environment of the MySQL cluster.
                    type: string
                  folderId:
                    description: The ID of the folder that the resource belongs to.
                      If it is not provided, the default provider folder is used.
                    type: string
                  folderIdRef:
                    description: Reference to a Folder in resourcemanager to populate
                      folderId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: Resolution specifies whether resolution of
                              this reference is required. The default is 'Required',
                              which means the reconcile will fail if the reference
                              cannot be resolved. 'Optional' means this reference
                              will be a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: Resolve specifies when this reference should
                              be resolved. The default is 'IfNotPresent', which will
                              attempt to resolve the reference only when the corresponding
                              field is not present. Use 'Always' to resolve the reference
                              on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  folderIdSelector:
                    description: Selector for a Folder in resourcemanager to populate
                      folderId.
                    properties:
                      matchControllerRef:
                        description: MatchControllerRef ensures an object with the
                          same controller reference as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: Resolution specifies whether resolution of
                              this reference is required. The default is 'Required',
                              which means the reconcile will fail if the reference
                              cannot be resolved. 'Optional' means this reference
                              will be a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: Resolve specifies when this reference should
                              be resolved. The default is 'IfNotPresent', which will
                              attempt to resolve the reference only when the corresponding
                              field is not present. Use 'Always' to resolve the reference
                              on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  host:
                    description: A host of the MySQL cluster. The structure is documented
                      below.
                    items:
                      properties:
                        assignPublicIp:
                          description: Sets whether the host should get a public IP
                            address. It can be changed on the fly only when name is
                            set.
                          type: boolean
                        backupPriority:
                          description: Host backup priority. Value is between 0 and
                            100, default is 0.
                          type: number
                        name:
                          description: Host state name. It should be set for all hosts
                            or unset for all hosts. This field can be used by another
                            host, to select which host will be its replication source.
                            Please refer to replication_source_name parameter.
                          type: string
                        priority:
                          description: Host master promotion priority. Value is between
                            0 and 100, default is 0.
                          type: number
                        replicationSourceName:
                          description: Host replication source name points to host's
                            name from which this host should replicate. When not set
                            then host in HA group. It works only when name is set.
                          type: string
                        subnetId:
                          description: The ID of the subnet, to which the host belongs.
                            The subnet must be a part of the network to which the
                            cluster belongs.
                          type: string
                        subnetIdRef:
                          description: Reference to a Subnet in vpc to populate subnetId.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: Resolution specifies whether resolution
                                    of this reference is required. The default is
                                    'Required', which means the reconcile will fail
                                    if the reference cannot be resolved. 'Optional'
                                    means this reference will be a no-op if it cannot
                                    be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: Resolve specifies when this reference
                                    should be resolved. The default is 'IfNotPresent',
                                    which will attempt to resolve the reference only
                                    when the corresponding field is not present. Use
                                    'Always' to resolve the reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        subnetIdSelector:
                          description: Selector for a Subnet in vpc to populate subnetId.
                          properties:
                            matchControllerRef:
                              description: MatchControllerRef ensures an object with
                                the same controller reference as the selecting object
                                is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: Resolution specifies whether resolution
                                    of this reference is required. The default is
                                    'Required', which means the reconcile will fail
                                    if the reference cannot be resolved. 'Optional'
                                    means this reference will be a no-op if it cannot
                                    be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: Resolve specifies when this reference
                                    should be resolved. The default is 'IfNotPresent',
                                    which will attempt to resolve the reference only
                                    when the corresponding field is not present. Use
                                    'Always' to resolve the reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                        zone:
                          description: The availability zone where the MySQL host
                            will be created.
                          type: string
                      required:
                      - zone
                      type: object
                    type: array
                  hostGroupIds:
                    items:
                      type: string
                    type: array
                  labels:
                    additionalProperties:
                      type: string
                    description: A set of key/value label pairs to assign to the MySQL
                      cluster.
                    type: object
                  maintenanceWindow:
                    description: Maintenance policy of the MySQL cluster. The structure
                      is documented below.
                    items:
                      properties:
                        day:
                          description: 'Day of the week (in DDD format). Allowed values:
                            "MON", "TUE", "WED", "THU", "FRI", "SAT", "SUN"'
                          type: string
                        hour:
                          description: Hour of the day in UTC (in HH format). Allowed
                            value is between 0 and 23.
                          type: number
                        type:
                          description: Type of maintenance window. Can be either ANYTIME
                            or WEEKLY. A day and hour of window need to be specified
                            with weekly window.
                          type: string
                      required:
                      - type
                      type: object
                    type: array
                  mysqlConfig:
                    additionalProperties:
                      type: string
                    description: MySQL cluster config. Detail info in "MySQL config"
                      section (documented below).
                    type: object
                  name:
                    description: Name of the MySQL cluster. Provided by the client
                      when the cluster is created.
                    type: string
                  networkId:
                    description: ID of the network, to which the MySQL cluster uses.
                    type: string
                  networkIdRef:
                    description: Reference to a Network in vpc to populate networkId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: Resolution specifies whether resolution of
                              this reference is required. The default is 'Required',
                              which means the reconcile will fail if the reference
                              cannot be resolved. 'Optional' means this reference
                              will be a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: Resolve specifies when this reference should
                              be resolved. The default is 'IfNotPresent', which will
                              attempt to resolve the reference only when the corresponding
                              field is not present. Use 'Always' to resolve the reference
                              on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  networkIdSelector:
                    description: Selector for a Network in vpc to populate networkId.
                    properties:
                      matchControllerRef:
                        description: MatchControllerRef ensures an object with the
                          same controller reference as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: Resolution specifies whether resolution of
                              this reference is required. The default is 'Required',
                              which means the reconcile will fail if the reference
                              cannot be resolved. 'Optional' means this reference
                              will be a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: Resolve specifies when this reference should
                              be resolved. The default is 'IfNotPresent', which will
                              attempt to resolve the reference only when the corresponding
                              field is not present. Use 'Always' to resolve the reference
                              on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  performanceDiagnostics:
                    description: Cluster performance diagnostics settings. The structure
                      is documented below. YC Documentation
                    items:
                      properties:
                        enabled:
                          description: Enable performance diagnostics
                          type: boolean
                        sessionsSamplingInterval:
                          description: Interval (in seconds) for my_stat_activity
                            sampling Acceptable values are 1 to 86400, inclusive.
                          type: number
                        statementsSamplingInterval:
                          description: Interval (in seconds) for my_stat_statements
                            sampling Acceptable values are 1 to 86400, inclusive.
                          type: number
                      required:
                      - sessionsSamplingInterval
                      - statementsSamplingInterval
                      type: object
                    type: array
                  resources:
                    description: Resources allocated to hosts of the MySQL cluster.
                      The structure is documented below.
                    items:
                      properties:
                        diskSize:
                          description: Volume of the storage available to a MySQL
                            host, in gigabytes.
                          type: number
                        diskTypeId:
                          description: Type of the storage of MySQL hosts.
                          type: string
                        resourcePresetId:
                          type: string
                      required:
                      - diskSize
                      - diskTypeId
                      - resourcePresetId
                      type: object
                    type: array
                  restore:
                    description: The cluster will be created from the specified backup.
                      The structure is documented below.
                    items:
                      properties:
                        backupId:
                          description: Backup ID. The cluster will be created from
                            the specified backup. How to get a list of MySQL backups.
                          type: string
                        time:
                          description: 'Timestamp of the moment to which the MySQL
                            cluster should be restored. (Format: "2006-01-02T15:04:05"
                            - UTC). When not set, current time is used.'
                          type: string
                      required:
                      - backupId
                      type: object
                    type: array
                  securityGroupIds:
                    description: A set of ids of security groups assigned to hosts
                      of the cluster.
                    items:
                      type: string
                    type: array
                  securityGroupIdsRefs:
                    description: References to SecurityGroup in vpc to populate securityGroupIds.
                    items:
                      description: A Reference to a named object.
                      properties:
                        name:
                          description: Name of the referenced object.
                          type: string
                        policy:
                          description: Policies for referencing.
                          properties:
                            resolution:
                              default: Required
                              description: Resolution specifies whether resolution
                                of this reference is required. The default is 'Required',
                                which means the reconcile will fail if the reference
                                cannot be resolved. 'Optional' means this reference
                                will be a no-op if it cannot be resolved.
                              enum:
                              - Required
                              - Optional
                              type: string
                            resolve:
                              description: Resolve specifies when this reference should
                                be resolved. The default is 'IfNotPresent', which
                                will attempt to resolve the reference only when the
                                corresponding field is not present. Use 'Always' to
                                resolve the reference on every reconcile.
                              enum:
                              - Always
                              - IfNotPresent
                              type: string
                          type: object
                      required:
                      - name
                      type: object
                    type: array
                  securityGroupIdsSelector:
                    description: Selector for a list of SecurityGroup in vpc to populate
                      securityGroupIds.
                    properties:
                      matchControllerRef:
                        description: MatchControllerRef ensures an object with the
                          same controller reference as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: Resolution specifies whether resolution of
                              this reference is required. The default is 'Required',
                              which means the reconcile will fail if the reference
                              cannot be resolved. 'Optional' means this reference
                              will be a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: Resolve specifies when this reference should
                              be resolved. The default is 'IfNotPresent', which will
                              attempt to resolve the reference only when the corresponding
                              field is not present. Use 'Always' to resolve the reference
                              on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  user:
                    description: (Deprecated) To manage users, please switch to using
                      a separate resource type yandex_mdb_mysql_user.
                    items:
                      properties:
                        authenticationPlugin:
                          description: 'Authentication plugin. Allowed values: MYSQL_NATIVE_PASSWORD,
                            CACHING_SHA2_PASSWORD, SHA256_PASSWORD (for version 5.7
                            MYSQL_NATIVE_PASSWORD, SHA256_PASSWORD)'
                          type: string
                        connectionLimits:
                          description: User's connection limits. The structure is
                            documented below. If the attribute is not specified there
                            will be no changes.
                          items:
                            properties:
                              maxConnectionsPerHour:
                                description: Max connections per hour.
                                type: number
                              maxQuestionsPerHour:
                                description: Max questions per hour.
                                type: number
                              maxUpdatesPerHour:
                                description: Max updates per hour.
                                type: number
                              maxUserConnections:
                                description: Max user connections.
                                type: number
                            type: object
                          type: array
                        globalPermissions:
                          description: 'List user''s global permissions Allowed permissions:  REPLICATION_CLIENT,
                            REPLICATION_SLAVE, PROCESS for clear list use empty list.
                            If the attribute is not specified there will be no changes.'
                          items:
                            type: string
                          type: array
                        name:
                          description: The name of the user.
                          type: string
                        passwordSecretRef:
                          description: The password of the user.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        permission:
                          description: Set of permissions granted to the user. The
                            structure is documented below.
                          items:
                            properties:
                              databaseName:
                                description: The name of the database that the permission
                                  grants access to.
                                type: string
                              roles:
                                description: 'List user''s roles in the database.
                                  Allowed roles: ALL,ALTER,ALTER_ROUTINE,CREATE,CREATE_ROUTINE,CREATE_TEMPORARY_TABLES,
                                  CREATE_VIEW,DELETE,DROP,EVENT,EXECUTE,INDEX,INSERT,LOCK_TABLES,SELECT,SHOW_VIEW,TRIGGER,UPDATE.'
                                items:
                                  type: string
                                type: array
                            required:
                            - databaseName
                            type: object
                          type: array
                      required:
                      - name
                      - passwordSecretRef
                      type: object
                    type: array
                  version:
                    description: 'Version of the MySQL cluster. (allowed versions
                      are: 5.7, 8.0)'
                    type: string
                type: object
              managementPolicy:
                default: FullControl
                description: 'THIS IS AN ALPHA FIELD. Do not use it in production.
                  It is not honored unless the relevant Crossplane feature flag is
                  enabled, and may be changed or removed without notice. ManagementPolicy
                  specifies the level of control Crossplane has over the managed external
                  resource. This field is planned to replace the DeletionPolicy field
                  in a future release. Currently, both could be set independently
                  and non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223'
                enum:
                - FullControl
                - ObserveOnly
                - OrphanOnDelete
                type: string
              providerConfigRef:
                default:
                  name: default
                description: ProviderConfigReference specifies how the provider that
                  will be used to create, observe, update, and delete this managed
                  resource should be configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: Resolution specifies whether resolution of this
                          reference is required. The default is 'Required', which
                          means the reconcile will fail if the reference cannot be
                          resolved. 'Optional' means this reference will be a no-op
                          if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: Resolve specifies when this reference should
                          be resolved. The default is 'IfNotPresent', which will attempt
                          to resolve the reference only when the corresponding field
                          is not present. Use 'Always' to resolve the reference on
                          every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              providerRef:
                description: 'ProviderReference specifies the provider that will be
                  used to create, observe, update, and delete this managed resource.
                  Deprecated: Please use ProviderConfigReference, i.e. `providerConfigRef`'
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: Resolution specifies whether resolution of this
                          reference is required. The default is 'Required', which
                          means the reconcile will fail if the reference cannot be
                          resolved. 'Optional' means this reference will be a no-op
                          if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: Resolve specifies when this reference should
                          be resolved. The default is 'IfNotPresent', which will attempt
                          to resolve the reference only when the corresponding field
                          is not present. Use 'Always' to resolve the reference on
                          every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              publishConnectionDetailsTo:
                description: PublishConnectionDetailsTo specifies the connection secret
                  config which contains a name, metadata and a reference to secret
                  store config to which any connection details for this managed resource
                  should be written. Connection details frequently include the endpoint,
                  username, and password required to connect to the managed resource.
                properties:
                  configRef:
                    default:
                      name: default
                    description: SecretStoreConfigRef specifies which secret store
                      config should be used for this ConnectionSecret.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: Resolution specifies whether resolution of
                              this reference is required. The default is 'Required',
                              which means the reconcile will fail if the reference
                              cannot be resolved. 'Optional' means this reference
                              will be a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: Resolve specifies when this reference should
                              be resolved. The default is 'IfNotPresent', which will
                              attempt to resolve the reference only when the corresponding
                              field is not present. Use 'Always' to resolve the reference
                              on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  metadata:
                    description: Metadata is the metadata for connection secret.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: Annotations are the annotations to be added to
                          connection secret. - For Kubernetes secrets, this will be
                          used as "metadata.annotations". - It is up to Secret Store
                          implementation for others store types.
                        type: object
                      labels:
                        additionalProperties:
                          type: string
                        description: Labels are the labels/tags to be added to connection
                          secret. - For Kubernetes secrets, this will be used as "metadata.labels".
                          - It is up to Secret Store implementation for others store
                          types.
                        type: object
                      type:
                        description: Type is the SecretType for the connection secret.
                          - Only valid for Kubernetes Secret Stores.
                        type: string
                    type: object
                  name:
                    description: Name is the name of the connection secret.
                    type: string
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: WriteConnectionSecretToReference specifies the namespace
                  and name of a Secret to which any connection details for this managed
                  resource should be written. Connection details frequently include
                  the endpoint, username, and password required to connect to the
                  managed resource. This field is planned to be replaced in a future
                  release in favor of PublishConnectionDetailsTo. Currently, both
                  could be set independently and connection details would be published
                  to both without affecting each other.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: environment is a required parameter
              rule: self.managementPolicy == 'ObserveOnly' || has(self.forProvider.environment)
            - message: host is a required parameter
              rule: self.managementPolicy == 'ObserveOnly' || has(self.forProvider.host)
            - message: name is a required parameter
              rule: self.managementPolicy == 'ObserveOnly' || has(self.forProvider.name)
            - message: resources is a required parameter
              rule: self.managementPolicy == 'ObserveOnly' || has(self.forProvider.resources)
            - message: version is a required parameter
              rule: self.managementPolicy == 'ObserveOnly' || has(self.forProvider.version)
          status:
            description: MySQLClusterStatus defines the observed state of MySQLCluster.
            properties:
              atProvider:
                properties:
                  access:
                    description: Access policy to the MySQL cluster. The structure
                      is documented below.
                    items:
                      properties:
                        dataLens:
                          description: Allow access for Yandex DataLens.
                          type: boolean
                        dataTransfer:
                          description: Allow access for DataTransfer
                          type: boolean
                        webSql:
                          description: Allows access for SQL queries in the management
                            console.
                          type: boolean
                      type: object
                    type: array
                  allowRegenerationHost:
                    description: A host of the MySQL cluster. The structure is documented
                      below.
                    type: boolean
                  backupRetainPeriodDays:
                    description: The period in days during which backups are stored.
                    type: number
                  backupWindowStart:
                    description: Time to start the daily backup, in the UTC. The structure
                      is documented below.
                    items:
                      properties:
                        hours:
                          description: The hour at which backup will be started.
                          type: number
                        minutes:
                          description: The minute at which backup will be started.
                          type: number
                      type: object
                    type: array
                  createdAt:
                    description: Creation timestamp of the cluster.
                    type: string
                  database:
                    description: (Deprecated) To manage databases, please switch to
                      using a separate resource type yandex_mdb_mysql_databases.
                    items:
                      properties:
                        name:
                          description: The name of the database.
                          type: string
                      type: object
                    type: array
                  deletionProtection:
                    description: Inhibits deletion of the cluster.  Can be either
                      true or false.
                    type: boolean
                  description:
                    description: Description of the MySQL cluster.
                    type: string
                  environment:
                    description: Deployment environment of the MySQL cluster.
                    type: string
                  folderId:
                    description: The ID of the folder that the resource belongs to.
                      If it is not provided, the default provider folder is used.
                    type: string
                  health:
                    description: Aggregated health of the cluster.
                    type: string
                  host:
                    description: A host of the MySQL cluster. The structure is documented
                      below.
                    items:
                      properties:
                        assignPublicIp:
                          description: Sets whether the host should get a public IP
                            address. It can be changed on the fly only when name is
                            set.
                          type: boolean
                        backupPriority:
                          description: Host backup priority. Value is between 0 and
                            100, default is 0.
                          type: number
                        fqdn:
                          description: (Computed) The fully qualified domain name
                            of the host.
                          type: string
                        name:
                          description: Host state name. It should be set for all hosts
                            or unset for all hosts. This field can be used by another
                            host, to select which host will be its replication source.
                            Please refer to replication_source_name parameter.
                          type: string
                        priority:
                          description: Host master promotion priority. Value is between
                            0 and 100, default is 0.
                          type: number
                        replicationSource:
                          description: (Computed) Host replication source (fqdn),
                            when replication_source is empty then host is in HA group.
                          type: string
                        replicationSourceName:
                          description: Host replication source name points to host's
                            name from which this host should replicate. When not set
                            then host in HA group. It works only when name is set.
                          type: string
                        subnetId:
                          description: The ID of the subnet, to which the host belongs.
                            The subnet must be a part of the network to which the
                            cluster belongs.
                          type: string
                        zone:
                          description: The availability zone where the MySQL host
                            will be created.
                          type: string
                      type: object
                    type: array
                  hostGroupIds:
                    items:
                      type: string
                    type: array
                  id:
                    type: string
                  labels:
                    additionalProperties:
                      type: string
                    description: A set of key/value label pairs to assign to the MySQL
                      cluster.
                    type: object
                  maintenanceWindow:
                    description: Maintenance policy of the MySQL cluster. The structure
                      is documented below.
                    items:
                      properties:
                        day:
                          description: 'Day of the week (in DDD format). Allowed values:
                            "MON", "TUE", "WED", "THU", "FRI", "SAT", "SUN"'
                          type: string
                        hour:
                          description: Hour of the day in UTC (in HH format). Allowed
                            value is between 0 and 23.
                          type: number
                        type:
                          description: Type of maintenance window. Can be either ANYTIME
                            or WEEKLY. A day and hour of window need to be specified
                            with weekly window.
                          type: string
                      type: object
                    type: array
                  mysqlConfig:
                    additionalProperties:
                      type: string
                    description: MySQL cluster config. Detail info in "MySQL config"
                      section (documented below).
                    type: object
                  name:
                    description: Name of the MySQL cluster. Provided by the client
                      when the cluster is created.
                    type: string
                  networkId:
                    description: ID of the network, to which the MySQL cluster uses.
                    type: string
                  performanceDiagnostics:
                    description: Cluster performance diagnostics settings. The structure
                      is documented below. YC Documentation
                    items:
                      properties:
                        enabled:
                          description: Enable performance diagnostics
                          type: boolean
                        sessionsSamplingInterval:
                          description: Interval (in seconds) for my_stat_activity
                            sampling Acceptable values are 1 to 86400, inclusive.
                          type: number
                        statementsSamplingInterval:
                          description: Interval (in seconds) for my_stat_statements
                            sampling Acceptable values are 1 to 86400, inclusive.
                          type: number
                      type: object
                    type: array
                  resources:
                    description: Resources allocated to hosts of the MySQL cluster.
                      The structure is documented below.
                    items:
                      properties:
                        diskSize:
                          description: Volume of the storage available to a MySQL
                            host, in gigabytes.
                          type: number
                        diskTypeId:
                          description: Type of the storage of MySQL hosts.
                          type: string
                        resourcePresetId:
                          type: string
                      type: object
                    type: array
                  restore:
                    description: The cluster will be created from the specified backup.
                      The structure is documented below.
                    items:
                      properties:
                        backupId:
                          description: Backup ID. The cluster will be created from
                            the specified backup. How to get a list of MySQL backups.
                          type: string
                        time:
                          description: 'Timestamp of the moment to which the MySQL
                            cluster should be restored. (Format: "2006-01-02T15:04:05"
                            - UTC). When not set, current time is used.'
                          type: string
                      type: object
                    type: array
                  securityGroupIds:
                    description: A set of ids of security groups assigned to hosts
                      of the cluster.
                    items:
                      type: string
                    type: array
                  status:
                    description: Status of the cluster.
                    type: string
                  user:
                    description: (Deprecated) To manage users, please switch to using
                      a separate resource type yandex_mdb_mysql_user.
                    items:
                      properties:
                        authenticationPlugin:
                          description: 'Authentication plugin. Allowed values: MYSQL_NATIVE_PASSWORD,
                            CACHING_SHA2_PASSWORD, SHA256_PASSWORD (for version 5.7
                            MYSQL_NATIVE_PASSWORD, SHA256_PASSWORD)'
                          type: string
                        connectionLimits:
                          description: User's connection limits. The structure is
                            documented below. If the attribute is not specified there
                            will be no changes.
                          items:
                            properties:
                              maxConnectionsPerHour:
                                description: Max connections per hour.
                                type: number
                              maxQuestionsPerHour:
                                description: Max questions per hour.
                                type: number
                              maxUpdatesPerHour:
                                description: Max updates per hour.
                                type: number
                              maxUserConnections:
                                description: Max user connections.
                                type: number
                            type: object
                          type: array
                        globalPermissions:
                          description: 'List user''s global permissions Allowed permissions:  REPLICATION_CLIENT,
                            REPLICATION_SLAVE, PROCESS for clear list use empty list.
                            If the attribute is not specified there will be no changes.'
                          items:
                            type: string
                          type: array
                        name:
                          description: The name of the user.
                          type: string
                        permission:
                          description: Set of permissions granted to the user. The
                            structure is documented below.
                          items:
                            properties:
                              databaseName:
                                description: The name of the database that the permission
                                  grants access to.
                                type: string
                              roles:
                                description: 'List user''s roles in the database.
                                  Allowed roles: ALL,ALTER,ALTER_ROUTINE,CREATE,CREATE_ROUTINE,CREATE_TEMPORARY_TABLES,
                                  CREATE_VIEW,DELETE,DROP,EVENT,EXECUTE,INDEX,INSERT,LOCK_TABLES,SELECT,SHOW_VIEW,TRIGGER,UPDATE.'
                                items:
                                  type: string
                                type: array
                            type: object
                          type: array
                      type: object
                    type: array
                  version:
                    description: 'Version of the MySQL cluster. (allowed versions
                      are: 5.7, 8.0)'
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: LastTransitionTime is the last time this condition
                        transitioned from one status to another.
                      format: date-time
                      type: string
                    message:
                      description: A Message containing details about this condition's
                        last transition from one status to another, if any.
                      type: string
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: Type of this condition. At most one of each condition
                        type may apply to a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}

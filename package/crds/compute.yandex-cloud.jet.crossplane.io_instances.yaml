---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: instances.compute.yandex-cloud.jet.crossplane.io
spec:
  group: compute.yandex-cloud.jet.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - yandex-cloud
    kind: Instance
    listKind: InstanceList
    plural: instances
    singular: instance
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Instance is the Schema for the Instances API. Manages a VM instance
          resource.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: InstanceSpec defines the desired state of Instance
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  allowRecreate:
                    description: Default 5 minutes
                    type: boolean
                  allowStoppingForUpdate:
                    description: If you try to update a property that requires stopping
                      the instance without setting this field, the update will fail.
                    type: boolean
                  bootDisk:
                    description: The boot disk for the instance. The structure is
                      documented below.
                    items:
                      properties:
                        autoDelete:
                          description: Defines whether the disk will be auto-deleted
                            when the instance is deleted. The default value is True.
                          type: boolean
                        deviceName:
                          description: Name that can be used to access an attached
                            disk.
                          type: string
                        diskId:
                          description: The ID of the existing disk (such as those
                            managed by yandex_compute_disk) to attach as a boot disk.
                          type: string
                        diskIdRef:
                          description: Reference to a Disk in compute to populate
                            diskId.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        diskIdSelector:
                          description: Selector for a Disk in compute to populate
                            diskId.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                        initializeParams:
                          description: Parameters for a new disk that will be created
                            alongside the new instance. Either initialize_params or
                            disk_id must be set. The structure is documented below.
                          items:
                            properties:
                              blockSize:
                                description: Block size of the disk, specified in
                                  bytes.
                                type: number
                              description:
                                description: Description of the boot disk.
                                type: string
                              imageId:
                                description: A disk image to initialize this disk
                                  from.
                                type: string
                              name:
                                description: Resource name.
                                type: string
                              size:
                                description: Size of the disk in GB.
                                type: number
                              snapshotId:
                                description: A snapshot to initialize this disk from.
                                type: string
                              type:
                                description: Disk type.
                                type: string
                            type: object
                          type: array
                        mode:
                          description: Type of access to the disk resource. By default,
                            a disk is attached in READ_WRITE mode.
                          type: string
                      type: object
                    type: array
                  description:
                    description: Description of the instance.
                    type: string
                  filesystem:
                    description: List of filesystems that are attached to the instance.
                      Structure is documented below.
                    items:
                      properties:
                        deviceName:
                          description: Name of the device representing the filesystem
                            on the instance.
                          type: string
                        filesystemId:
                          description: ID of the filesystem that should be attached.
                          type: string
                        filesystemIdRef:
                          description: Reference to a Filesystem in compute to populate
                            filesystemId.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        filesystemIdSelector:
                          description: Selector for a Filesystem in compute to populate
                            filesystemId.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                        mode:
                          description: Mode of access to the filesystem that should
                            be attached. By default, filesystem is attached in READ_WRITE
                            mode.
                          type: string
                      type: object
                    type: array
                  folderId:
                    description: The ID of the folder that the resource belongs to.
                      If it is not provided, the default provider folder is used.
                    type: string
                  folderIdRef:
                    description: Reference to a Folder in resourcemanager to populate
                      folderId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  folderIdSelector:
                    description: Selector for a Folder in resourcemanager to populate
                      folderId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  gpuClusterId:
                    description: ID of the GPU cluster to attach this instance to.
                      The GPU cluster must exist in the same zone as the instance.
                    type: string
                  hostname:
                    description: Host name for the instance. This field is used to
                      generate the instance fqdn value. The host name must be unique
                      within the network and region. If not specified, the host name
                      will be equal to id of the instance and fqdn will be <id>.auto.internal.
                      Otherwise FQDN will be <hostname>.<region_id>.internal.
                    type: string
                  labels:
                    additionalProperties:
                      type: string
                    description: A set of key/value label pairs to assign to the instance.
                    type: object
                    x-kubernetes-map-type: granular
                  localDisk:
                    description: List of local disks that are attached to the instance.
                      Structure is documented below.
                    items:
                      properties:
                        sizeBytes:
                          description: Size of the disk, specified in bytes.
                          type: number
                      type: object
                    type: array
                  maintenanceGracePeriod:
                    description: Time between notification via metadata service and
                      maintenance. E.g., 60s.
                    type: string
                  maintenancePolicy:
                    description: 'Behaviour on maintenance events. The default is
                      unspecified. Values: unspecified, migrate, restart.'
                    type: string
                  metadata:
                    additionalProperties:
                      type: string
                    description: Metadata key/value pairs to make available from within
                      the instance.
                    type: object
                    x-kubernetes-map-type: granular
                  metadataOptions:
                    description: Options allow user to configure access to instance's
                      metadata
                    items:
                      properties:
                        awsV1HttpEndpoint:
                          type: number
                        awsV1HttpToken:
                          type: number
                        gceHttpEndpoint:
                          type: number
                        gceHttpToken:
                          type: number
                      type: object
                    type: array
                  name:
                    description: Resource name.
                    type: string
                  networkAccelerationType:
                    description: 'Type of network acceleration. The default is standard.
                      Values: standard, software_accelerated'
                    type: string
                  networkInterface:
                    description: Networks to attach to the instance. This can be specified
                      multiple times. The structure is documented below.
                    items:
                      properties:
                        dnsRecord:
                          description: List of configurations for creating ipv4 DNS
                            records. The structure is documented below.
                          items:
                            properties:
                              dnsZoneId:
                                description: DNS zone ID (if not set, private zone
                                  used).
                                type: string
                              fqdn:
                                description: DNS record FQDN (must have a dot at the
                                  end).
                                type: string
                              ptr:
                                description: When set to true, also create a PTR DNS
                                  record.
                                type: boolean
                              ttl:
                                description: DNS record TTL. in seconds
                                type: number
                            type: object
                          type: array
                        index:
                          description: Index of network interface, will be calculated
                            automatically for instance create or update operations
                            if not specified. Required for attach/detach operations.
                          type: number
                        ipAddress:
                          description: The private IP address to assign to the instance.
                            If empty, the address will be automatically assigned from
                            the specified subnet.
                          type: string
                        ipv4:
                          description: Allocate an IPv4 address for the interface.
                            The default value is true.
                          type: boolean
                        ipv6:
                          description: If true, allocate an IPv6 address for the interface.
                            The address will be automatically assigned from the specified
                            subnet.
                          type: boolean
                        ipv6Address:
                          description: The private IPv6 address to assign to the instance.
                          type: string
                        ipv6DnsRecord:
                          description: List of configurations for creating ipv6 DNS
                            records. The structure is documented below.
                          items:
                            properties:
                              dnsZoneId:
                                description: DNS zone ID (if not set, private zone
                                  used).
                                type: string
                              fqdn:
                                description: DNS record FQDN (must have a dot at the
                                  end).
                                type: string
                              ptr:
                                description: When set to true, also create a PTR DNS
                                  record.
                                type: boolean
                              ttl:
                                description: DNS record TTL. in seconds
                                type: number
                            type: object
                          type: array
                        nat:
                          description: Provide a public address, for instance, to
                            access the internet over NAT.
                          type: boolean
                        natDnsRecord:
                          description: List of configurations for creating ipv4 NAT
                            DNS records. The structure is documented below.
                          items:
                            properties:
                              dnsZoneId:
                                description: DNS zone ID (if not set, private zone
                                  used).
                                type: string
                              fqdn:
                                description: DNS record FQDN (must have a dot at the
                                  end).
                                type: string
                              ptr:
                                description: When set to true, also create a PTR DNS
                                  record.
                                type: boolean
                              ttl:
                                description: DNS record TTL. in seconds
                                type: number
                            type: object
                          type: array
                        natIpAddress:
                          description: Provide a public address, for instance, to
                            access the internet over NAT. Address should be already
                            reserved in web UI.
                          type: string
                        securityGroupIds:
                          description: Security group ids for network interface.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                        securityGroupIdsRefs:
                          description: References to SecurityGroup in vpc to populate
                            securityGroupIds.
                          items:
                            description: A Reference to a named object.
                            properties:
                              name:
                                description: Name of the referenced object.
                                type: string
                              policy:
                                description: Policies for referencing.
                                properties:
                                  resolution:
                                    default: Required
                                    description: |-
                                      Resolution specifies whether resolution of this reference is required.
                                      The default is 'Required', which means the reconcile will fail if the
                                      reference cannot be resolved. 'Optional' means this reference will be
                                      a no-op if it cannot be resolved.
                                    enum:
                                    - Required
                                    - Optional
                                    type: string
                                  resolve:
                                    description: |-
                                      Resolve specifies when this reference should be resolved. The default
                                      is 'IfNotPresent', which will attempt to resolve the reference only when
                                      the corresponding field is not present. Use 'Always' to resolve the
                                      reference on every reconcile.
                                    enum:
                                    - Always
                                    - IfNotPresent
                                    type: string
                                type: object
                            required:
                            - name
                            type: object
                          type: array
                        securityGroupIdsSelector:
                          description: Selector for a list of SecurityGroup in vpc
                            to populate securityGroupIds.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                        subnetId:
                          description: ID of the subnet to attach this interface to.
                            The subnet must exist in the same zone where this instance
                            will be created.
                          type: string
                        subnetIdRef:
                          description: Reference to a Subnet in vpc to populate subnetId.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        subnetIdSelector:
                          description: Selector for a Subnet in vpc to populate subnetId.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                      type: object
                    type: array
                  placementPolicy:
                    description: The placement policy configuration. The structure
                      is documented below.
                    items:
                      properties:
                        hostAffinityRules:
                          description: List of host affinity rules. The structure
                            is documented below.
                          items:
                            properties:
                              key:
                                description: Affinity label or one of reserved values
                                  - yc.hostId, yc.hostGroupId.
                                type: string
                              op:
                                description: Affinity action. The only value supported
                                  is IN.
                                type: string
                              values:
                                items:
                                  type: string
                                type: array
                            type: object
                          type: array
                        placementGroupId:
                          description: Specifies the id of the Placement Group to
                            assign to the instance.
                          type: string
                        placementGroupIdRef:
                          description: Reference to a PlacementGroup in compute to
                            populate placementGroupId.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        placementGroupIdSelector:
                          description: Selector for a PlacementGroup in compute to
                            populate placementGroupId.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                        placementGroupPartition:
                          type: number
                      type: object
                    type: array
                  platformId:
                    description: The type of virtual machine to create. The default
                      is 'standard-v1'.
                    type: string
                  resources:
                    description: Compute resources that are allocated for the instance.
                      The structure is documented below.
                    items:
                      properties:
                        coreFraction:
                          description: If provided, specifies baseline performance
                            for a core as a percent.
                          type: number
                        cores:
                          description: CPU cores for the instance.
                          type: number
                        gpus:
                          description: If provided, specifies the number of GPU devices
                            for the instance
                          type: number
                        memory:
                          description: Memory size in GB.
                          type: number
                      type: object
                    type: array
                  schedulingPolicy:
                    description: Scheduling policy configuration. The structure is
                      documented below.
                    items:
                      properties:
                        preemptible:
                          description: Specifies if the instance is preemptible. Defaults
                            to false.
                          type: boolean
                      type: object
                    type: array
                  secondaryDisk:
                    description: 'A set of disks to attach to the instance. The structure
                      is documented below. Note: The allow_stopping_for_update property
                      must be set to true in order to update this structure.'
                    items:
                      properties:
                        autoDelete:
                          description: Whether the disk is auto-deleted when the instance
                            is deleted. The default value is false.
                          type: boolean
                        deviceName:
                          description: Name that can be used to access an attached
                            disk under /dev/disk/by-id/.
                          type: string
                        diskId:
                          description: ID of the disk that is attached to the instance.
                          type: string
                        diskIdRef:
                          description: Reference to a Disk in compute to populate
                            diskId.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        diskIdSelector:
                          description: Selector for a Disk in compute to populate
                            diskId.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                        mode:
                          description: Type of access to the disk resource. By default,
                            a disk is attached in READ_WRITE mode.
                          type: string
                      type: object
                    type: array
                  serviceAccountId:
                    description: ID of the service account authorized for this instance.
                    type: string
                  serviceAccountIdRef:
                    description: Reference to a ServiceAccount in iam to populate
                      serviceAccountId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  serviceAccountIdSelector:
                    description: Selector for a ServiceAccount in iam to populate
                      serviceAccountId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  zone:
                    description: The availability zone where the virtual machine will
                      be created. If it is not provided, the default provider folder
                      is used.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  allowRecreate:
                    description: Default 5 minutes
                    type: boolean
                  allowStoppingForUpdate:
                    description: If you try to update a property that requires stopping
                      the instance without setting this field, the update will fail.
                    type: boolean
                  bootDisk:
                    description: The boot disk for the instance. The structure is
                      documented below.
                    items:
                      properties:
                        autoDelete:
                          description: Defines whether the disk will be auto-deleted
                            when the instance is deleted. The default value is True.
                          type: boolean
                        deviceName:
                          description: Name that can be used to access an attached
                            disk.
                          type: string
                        diskId:
                          description: The ID of the existing disk (such as those
                            managed by yandex_compute_disk) to attach as a boot disk.
                          type: string
                        diskIdRef:
                          description: Reference to a Disk in compute to populate
                            diskId.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        diskIdSelector:
                          description: Selector for a Disk in compute to populate
                            diskId.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                        initializeParams:
                          description: Parameters for a new disk that will be created
                            alongside the new instance. Either initialize_params or
                            disk_id must be set. The structure is documented below.
                          items:
                            properties:
                              blockSize:
                                description: Block size of the disk, specified in
                                  bytes.
                                type: number
                              description:
                                description: Description of the boot disk.
                                type: string
                              imageId:
                                description: A disk image to initialize this disk
                                  from.
                                type: string
                              name:
                                description: Resource name.
                                type: string
                              size:
                                description: Size of the disk in GB.
                                type: number
                              snapshotId:
                                description: A snapshot to initialize this disk from.
                                type: string
                              type:
                                description: Disk type.
                                type: string
                            type: object
                          type: array
                        mode:
                          description: Type of access to the disk resource. By default,
                            a disk is attached in READ_WRITE mode.
                          type: string
                      type: object
                    type: array
                  description:
                    description: Description of the instance.
                    type: string
                  filesystem:
                    description: List of filesystems that are attached to the instance.
                      Structure is documented below.
                    items:
                      properties:
                        deviceName:
                          description: Name of the device representing the filesystem
                            on the instance.
                          type: string
                        filesystemId:
                          description: ID of the filesystem that should be attached.
                          type: string
                        filesystemIdRef:
                          description: Reference to a Filesystem in compute to populate
                            filesystemId.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        filesystemIdSelector:
                          description: Selector for a Filesystem in compute to populate
                            filesystemId.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                        mode:
                          description: Mode of access to the filesystem that should
                            be attached. By default, filesystem is attached in READ_WRITE
                            mode.
                          type: string
                      type: object
                    type: array
                  folderId:
                    description: The ID of the folder that the resource belongs to.
                      If it is not provided, the default provider folder is used.
                    type: string
                  folderIdRef:
                    description: Reference to a Folder in resourcemanager to populate
                      folderId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  folderIdSelector:
                    description: Selector for a Folder in resourcemanager to populate
                      folderId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  gpuClusterId:
                    description: ID of the GPU cluster to attach this instance to.
                      The GPU cluster must exist in the same zone as the instance.
                    type: string
                  hostname:
                    description: Host name for the instance. This field is used to
                      generate the instance fqdn value. The host name must be unique
                      within the network and region. If not specified, the host name
                      will be equal to id of the instance and fqdn will be <id>.auto.internal.
                      Otherwise FQDN will be <hostname>.<region_id>.internal.
                    type: string
                  labels:
                    additionalProperties:
                      type: string
                    description: A set of key/value label pairs to assign to the instance.
                    type: object
                    x-kubernetes-map-type: granular
                  localDisk:
                    description: List of local disks that are attached to the instance.
                      Structure is documented below.
                    items:
                      properties:
                        sizeBytes:
                          description: Size of the disk, specified in bytes.
                          type: number
                      type: object
                    type: array
                  maintenanceGracePeriod:
                    description: Time between notification via metadata service and
                      maintenance. E.g., 60s.
                    type: string
                  maintenancePolicy:
                    description: 'Behaviour on maintenance events. The default is
                      unspecified. Values: unspecified, migrate, restart.'
                    type: string
                  metadata:
                    additionalProperties:
                      type: string
                    description: Metadata key/value pairs to make available from within
                      the instance.
                    type: object
                    x-kubernetes-map-type: granular
                  metadataOptions:
                    description: Options allow user to configure access to instance's
                      metadata
                    items:
                      properties:
                        awsV1HttpEndpoint:
                          type: number
                        awsV1HttpToken:
                          type: number
                        gceHttpEndpoint:
                          type: number
                        gceHttpToken:
                          type: number
                      type: object
                    type: array
                  name:
                    description: Resource name.
                    type: string
                  networkAccelerationType:
                    description: 'Type of network acceleration. The default is standard.
                      Values: standard, software_accelerated'
                    type: string
                  networkInterface:
                    description: Networks to attach to the instance. This can be specified
                      multiple times. The structure is documented below.
                    items:
                      properties:
                        dnsRecord:
                          description: List of configurations for creating ipv4 DNS
                            records. The structure is documented below.
                          items:
                            properties:
                              dnsZoneId:
                                description: DNS zone ID (if not set, private zone
                                  used).
                                type: string
                              fqdn:
                                description: DNS record FQDN (must have a dot at the
                                  end).
                                type: string
                              ptr:
                                description: When set to true, also create a PTR DNS
                                  record.
                                type: boolean
                              ttl:
                                description: DNS record TTL. in seconds
                                type: number
                            type: object
                          type: array
                        index:
                          description: Index of network interface, will be calculated
                            automatically for instance create or update operations
                            if not specified. Required for attach/detach operations.
                          type: number
                        ipAddress:
                          description: The private IP address to assign to the instance.
                            If empty, the address will be automatically assigned from
                            the specified subnet.
                          type: string
                        ipv4:
                          description: Allocate an IPv4 address for the interface.
                            The default value is true.
                          type: boolean
                        ipv6:
                          description: If true, allocate an IPv6 address for the interface.
                            The address will be automatically assigned from the specified
                            subnet.
                          type: boolean
                        ipv6Address:
                          description: The private IPv6 address to assign to the instance.
                          type: string
                        ipv6DnsRecord:
                          description: List of configurations for creating ipv6 DNS
                            records. The structure is documented below.
                          items:
                            properties:
                              dnsZoneId:
                                description: DNS zone ID (if not set, private zone
                                  used).
                                type: string
                              fqdn:
                                description: DNS record FQDN (must have a dot at the
                                  end).
                                type: string
                              ptr:
                                description: When set to true, also create a PTR DNS
                                  record.
                                type: boolean
                              ttl:
                                description: DNS record TTL. in seconds
                                type: number
                            type: object
                          type: array
                        nat:
                          description: Provide a public address, for instance, to
                            access the internet over NAT.
                          type: boolean
                        natDnsRecord:
                          description: List of configurations for creating ipv4 NAT
                            DNS records. The structure is documented below.
                          items:
                            properties:
                              dnsZoneId:
                                description: DNS zone ID (if not set, private zone
                                  used).
                                type: string
                              fqdn:
                                description: DNS record FQDN (must have a dot at the
                                  end).
                                type: string
                              ptr:
                                description: When set to true, also create a PTR DNS
                                  record.
                                type: boolean
                              ttl:
                                description: DNS record TTL. in seconds
                                type: number
                            type: object
                          type: array
                        natIpAddress:
                          description: Provide a public address, for instance, to
                            access the internet over NAT. Address should be already
                            reserved in web UI.
                          type: string
                        securityGroupIds:
                          description: Security group ids for network interface.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                        securityGroupIdsRefs:
                          description: References to SecurityGroup in vpc to populate
                            securityGroupIds.
                          items:
                            description: A Reference to a named object.
                            properties:
                              name:
                                description: Name of the referenced object.
                                type: string
                              policy:
                                description: Policies for referencing.
                                properties:
                                  resolution:
                                    default: Required
                                    description: |-
                                      Resolution specifies whether resolution of this reference is required.
                                      The default is 'Required', which means the reconcile will fail if the
                                      reference cannot be resolved. 'Optional' means this reference will be
                                      a no-op if it cannot be resolved.
                                    enum:
                                    - Required
                                    - Optional
                                    type: string
                                  resolve:
                                    description: |-
                                      Resolve specifies when this reference should be resolved. The default
                                      is 'IfNotPresent', which will attempt to resolve the reference only when
                                      the corresponding field is not present. Use 'Always' to resolve the
                                      reference on every reconcile.
                                    enum:
                                    - Always
                                    - IfNotPresent
                                    type: string
                                type: object
                            required:
                            - name
                            type: object
                          type: array
                        securityGroupIdsSelector:
                          description: Selector for a list of SecurityGroup in vpc
                            to populate securityGroupIds.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                        subnetId:
                          description: ID of the subnet to attach this interface to.
                            The subnet must exist in the same zone where this instance
                            will be created.
                          type: string
                        subnetIdRef:
                          description: Reference to a Subnet in vpc to populate subnetId.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        subnetIdSelector:
                          description: Selector for a Subnet in vpc to populate subnetId.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                      type: object
                    type: array
                  placementPolicy:
                    description: The placement policy configuration. The structure
                      is documented below.
                    items:
                      properties:
                        hostAffinityRules:
                          description: List of host affinity rules. The structure
                            is documented below.
                          items:
                            properties:
                              key:
                                description: Affinity label or one of reserved values
                                  - yc.hostId, yc.hostGroupId.
                                type: string
                              op:
                                description: Affinity action. The only value supported
                                  is IN.
                                type: string
                              values:
                                items:
                                  type: string
                                type: array
                            type: object
                          type: array
                        placementGroupId:
                          description: Specifies the id of the Placement Group to
                            assign to the instance.
                          type: string
                        placementGroupIdRef:
                          description: Reference to a PlacementGroup in compute to
                            populate placementGroupId.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        placementGroupIdSelector:
                          description: Selector for a PlacementGroup in compute to
                            populate placementGroupId.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                        placementGroupPartition:
                          type: number
                      type: object
                    type: array
                  platformId:
                    description: The type of virtual machine to create. The default
                      is 'standard-v1'.
                    type: string
                  resources:
                    description: Compute resources that are allocated for the instance.
                      The structure is documented below.
                    items:
                      properties:
                        coreFraction:
                          description: If provided, specifies baseline performance
                            for a core as a percent.
                          type: number
                        cores:
                          description: CPU cores for the instance.
                          type: number
                        gpus:
                          description: If provided, specifies the number of GPU devices
                            for the instance
                          type: number
                        memory:
                          description: Memory size in GB.
                          type: number
                      type: object
                    type: array
                  schedulingPolicy:
                    description: Scheduling policy configuration. The structure is
                      documented below.
                    items:
                      properties:
                        preemptible:
                          description: Specifies if the instance is preemptible. Defaults
                            to false.
                          type: boolean
                      type: object
                    type: array
                  secondaryDisk:
                    description: 'A set of disks to attach to the instance. The structure
                      is documented below. Note: The allow_stopping_for_update property
                      must be set to true in order to update this structure.'
                    items:
                      properties:
                        autoDelete:
                          description: Whether the disk is auto-deleted when the instance
                            is deleted. The default value is false.
                          type: boolean
                        deviceName:
                          description: Name that can be used to access an attached
                            disk under /dev/disk/by-id/.
                          type: string
                        diskId:
                          description: ID of the disk that is attached to the instance.
                          type: string
                        diskIdRef:
                          description: Reference to a Disk in compute to populate
                            diskId.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        diskIdSelector:
                          description: Selector for a Disk in compute to populate
                            diskId.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                        mode:
                          description: Type of access to the disk resource. By default,
                            a disk is attached in READ_WRITE mode.
                          type: string
                      type: object
                    type: array
                  serviceAccountId:
                    description: ID of the service account authorized for this instance.
                    type: string
                  serviceAccountIdRef:
                    description: Reference to a ServiceAccount in iam to populate
                      serviceAccountId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  serviceAccountIdSelector:
                    description: Selector for a ServiceAccount in iam to populate
                      serviceAccountId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  zone:
                    description: The availability zone where the virtual machine will
                      be created. If it is not provided, the default provider folder
                      is used.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              publishConnectionDetailsTo:
                description: |-
                  PublishConnectionDetailsTo specifies the connection secret config which
                  contains a name, metadata and a reference to secret store config to
                  which any connection details for this managed resource should be written.
                  Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  configRef:
                    default:
                      name: default
                    description: |-
                      SecretStoreConfigRef specifies which secret store config should be used
                      for this ConnectionSecret.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  metadata:
                    description: Metadata is the metadata for connection secret.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: |-
                          Annotations are the annotations to be added to connection secret.
                          - For Kubernetes secrets, this will be used as "metadata.annotations".
                          - It is up to Secret Store implementation for others store types.
                        type: object
                      labels:
                        additionalProperties:
                          type: string
                        description: |-
                          Labels are the labels/tags to be added to connection secret.
                          - For Kubernetes secrets, this will be used as "metadata.labels".
                          - It is up to Secret Store implementation for others store types.
                        type: object
                      type:
                        description: |-
                          Type is the SecretType for the connection secret.
                          - Only valid for Kubernetes Secret Stores.
                        type: string
                    type: object
                  name:
                    description: Name is the name of the connection secret.
                    type: string
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                  This field is planned to be replaced in a future release in favor of
                  PublishConnectionDetailsTo. Currently, both could be set independently
                  and connection details would be published to both without affecting
                  each other.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.bootDisk is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.bootDisk)
                || (has(self.initProvider) && has(self.initProvider.bootDisk))'
            - message: spec.forProvider.networkInterface is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.networkInterface)
                || (has(self.initProvider) && has(self.initProvider.networkInterface))'
            - message: spec.forProvider.resources is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.resources)
                || (has(self.initProvider) && has(self.initProvider.resources))'
          status:
            description: InstanceStatus defines the observed state of Instance.
            properties:
              atProvider:
                properties:
                  allowRecreate:
                    description: Default 5 minutes
                    type: boolean
                  allowStoppingForUpdate:
                    description: If you try to update a property that requires stopping
                      the instance without setting this field, the update will fail.
                    type: boolean
                  bootDisk:
                    description: The boot disk for the instance. The structure is
                      documented below.
                    items:
                      properties:
                        autoDelete:
                          description: Defines whether the disk will be auto-deleted
                            when the instance is deleted. The default value is True.
                          type: boolean
                        deviceName:
                          description: Name that can be used to access an attached
                            disk.
                          type: string
                        diskId:
                          description: The ID of the existing disk (such as those
                            managed by yandex_compute_disk) to attach as a boot disk.
                          type: string
                        initializeParams:
                          description: Parameters for a new disk that will be created
                            alongside the new instance. Either initialize_params or
                            disk_id must be set. The structure is documented below.
                          items:
                            properties:
                              blockSize:
                                description: Block size of the disk, specified in
                                  bytes.
                                type: number
                              description:
                                description: Description of the boot disk.
                                type: string
                              imageId:
                                description: A disk image to initialize this disk
                                  from.
                                type: string
                              name:
                                description: Resource name.
                                type: string
                              size:
                                description: Size of the disk in GB.
                                type: number
                              snapshotId:
                                description: A snapshot to initialize this disk from.
                                type: string
                              type:
                                description: Disk type.
                                type: string
                            type: object
                          type: array
                        mode:
                          description: Type of access to the disk resource. By default,
                            a disk is attached in READ_WRITE mode.
                          type: string
                      type: object
                    type: array
                  createdAt:
                    description: Creation timestamp of the instance.
                    type: string
                  description:
                    description: Description of the instance.
                    type: string
                  filesystem:
                    description: List of filesystems that are attached to the instance.
                      Structure is documented below.
                    items:
                      properties:
                        deviceName:
                          description: Name of the device representing the filesystem
                            on the instance.
                          type: string
                        filesystemId:
                          description: ID of the filesystem that should be attached.
                          type: string
                        mode:
                          description: Mode of access to the filesystem that should
                            be attached. By default, filesystem is attached in READ_WRITE
                            mode.
                          type: string
                      type: object
                    type: array
                  folderId:
                    description: The ID of the folder that the resource belongs to.
                      If it is not provided, the default provider folder is used.
                    type: string
                  fqdn:
                    description: The fully qualified DNS name of this instance.
                    type: string
                  gpuClusterId:
                    description: ID of the GPU cluster to attach this instance to.
                      The GPU cluster must exist in the same zone as the instance.
                    type: string
                  hardwareGeneration:
                    items:
                      properties:
                        generation2Features:
                          items:
                            type: object
                          type: array
                        legacyFeatures:
                          items:
                            properties:
                              pciTopology:
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  hostname:
                    description: Host name for the instance. This field is used to
                      generate the instance fqdn value. The host name must be unique
                      within the network and region. If not specified, the host name
                      will be equal to id of the instance and fqdn will be <id>.auto.internal.
                      Otherwise FQDN will be <hostname>.<region_id>.internal.
                    type: string
                  id:
                    type: string
                  labels:
                    additionalProperties:
                      type: string
                    description: A set of key/value label pairs to assign to the instance.
                    type: object
                    x-kubernetes-map-type: granular
                  localDisk:
                    description: List of local disks that are attached to the instance.
                      Structure is documented below.
                    items:
                      properties:
                        deviceName:
                          description: The name of the local disk device.
                          type: string
                        sizeBytes:
                          description: Size of the disk, specified in bytes.
                          type: number
                      type: object
                    type: array
                  maintenanceGracePeriod:
                    description: Time between notification via metadata service and
                      maintenance. E.g., 60s.
                    type: string
                  maintenancePolicy:
                    description: 'Behaviour on maintenance events. The default is
                      unspecified. Values: unspecified, migrate, restart.'
                    type: string
                  metadata:
                    additionalProperties:
                      type: string
                    description: Metadata key/value pairs to make available from within
                      the instance.
                    type: object
                    x-kubernetes-map-type: granular
                  metadataOptions:
                    description: Options allow user to configure access to instance's
                      metadata
                    items:
                      properties:
                        awsV1HttpEndpoint:
                          type: number
                        awsV1HttpToken:
                          type: number
                        gceHttpEndpoint:
                          type: number
                        gceHttpToken:
                          type: number
                      type: object
                    type: array
                  name:
                    description: Resource name.
                    type: string
                  networkAccelerationType:
                    description: 'Type of network acceleration. The default is standard.
                      Values: standard, software_accelerated'
                    type: string
                  networkInterface:
                    description: Networks to attach to the instance. This can be specified
                      multiple times. The structure is documented below.
                    items:
                      properties:
                        dnsRecord:
                          description: List of configurations for creating ipv4 DNS
                            records. The structure is documented below.
                          items:
                            properties:
                              dnsZoneId:
                                description: DNS zone ID (if not set, private zone
                                  used).
                                type: string
                              fqdn:
                                description: DNS record FQDN (must have a dot at the
                                  end).
                                type: string
                              ptr:
                                description: When set to true, also create a PTR DNS
                                  record.
                                type: boolean
                              ttl:
                                description: DNS record TTL. in seconds
                                type: number
                            type: object
                          type: array
                        index:
                          description: Index of network interface, will be calculated
                            automatically for instance create or update operations
                            if not specified. Required for attach/detach operations.
                          type: number
                        ipAddress:
                          description: The private IP address to assign to the instance.
                            If empty, the address will be automatically assigned from
                            the specified subnet.
                          type: string
                        ipv4:
                          description: Allocate an IPv4 address for the interface.
                            The default value is true.
                          type: boolean
                        ipv6:
                          description: If true, allocate an IPv6 address for the interface.
                            The address will be automatically assigned from the specified
                            subnet.
                          type: boolean
                        ipv6Address:
                          description: The private IPv6 address to assign to the instance.
                          type: string
                        ipv6DnsRecord:
                          description: List of configurations for creating ipv6 DNS
                            records. The structure is documented below.
                          items:
                            properties:
                              dnsZoneId:
                                description: DNS zone ID (if not set, private zone
                                  used).
                                type: string
                              fqdn:
                                description: DNS record FQDN (must have a dot at the
                                  end).
                                type: string
                              ptr:
                                description: When set to true, also create a PTR DNS
                                  record.
                                type: boolean
                              ttl:
                                description: DNS record TTL. in seconds
                                type: number
                            type: object
                          type: array
                        macAddress:
                          type: string
                        nat:
                          description: Provide a public address, for instance, to
                            access the internet over NAT.
                          type: boolean
                        natDnsRecord:
                          description: List of configurations for creating ipv4 NAT
                            DNS records. The structure is documented below.
                          items:
                            properties:
                              dnsZoneId:
                                description: DNS zone ID (if not set, private zone
                                  used).
                                type: string
                              fqdn:
                                description: DNS record FQDN (must have a dot at the
                                  end).
                                type: string
                              ptr:
                                description: When set to true, also create a PTR DNS
                                  record.
                                type: boolean
                              ttl:
                                description: DNS record TTL. in seconds
                                type: number
                            type: object
                          type: array
                        natIpAddress:
                          description: Provide a public address, for instance, to
                            access the internet over NAT. Address should be already
                            reserved in web UI.
                          type: string
                        natIpVersion:
                          type: string
                        securityGroupIds:
                          description: Security group ids for network interface.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                        subnetId:
                          description: ID of the subnet to attach this interface to.
                            The subnet must exist in the same zone where this instance
                            will be created.
                          type: string
                      type: object
                    type: array
                  placementPolicy:
                    description: The placement policy configuration. The structure
                      is documented below.
                    items:
                      properties:
                        hostAffinityRules:
                          description: List of host affinity rules. The structure
                            is documented below.
                          items:
                            properties:
                              key:
                                description: Affinity label or one of reserved values
                                  - yc.hostId, yc.hostGroupId.
                                type: string
                              op:
                                description: Affinity action. The only value supported
                                  is IN.
                                type: string
                              values:
                                items:
                                  type: string
                                type: array
                            type: object
                          type: array
                        placementGroupId:
                          description: Specifies the id of the Placement Group to
                            assign to the instance.
                          type: string
                        placementGroupPartition:
                          type: number
                      type: object
                    type: array
                  platformId:
                    description: The type of virtual machine to create. The default
                      is 'standard-v1'.
                    type: string
                  resources:
                    description: Compute resources that are allocated for the instance.
                      The structure is documented below.
                    items:
                      properties:
                        coreFraction:
                          description: If provided, specifies baseline performance
                            for a core as a percent.
                          type: number
                        cores:
                          description: CPU cores for the instance.
                          type: number
                        gpus:
                          description: If provided, specifies the number of GPU devices
                            for the instance
                          type: number
                        memory:
                          description: Memory size in GB.
                          type: number
                      type: object
                    type: array
                  schedulingPolicy:
                    description: Scheduling policy configuration. The structure is
                      documented below.
                    items:
                      properties:
                        preemptible:
                          description: Specifies if the instance is preemptible. Defaults
                            to false.
                          type: boolean
                      type: object
                    type: array
                  secondaryDisk:
                    description: 'A set of disks to attach to the instance. The structure
                      is documented below. Note: The allow_stopping_for_update property
                      must be set to true in order to update this structure.'
                    items:
                      properties:
                        autoDelete:
                          description: Whether the disk is auto-deleted when the instance
                            is deleted. The default value is false.
                          type: boolean
                        deviceName:
                          description: Name that can be used to access an attached
                            disk under /dev/disk/by-id/.
                          type: string
                        diskId:
                          description: ID of the disk that is attached to the instance.
                          type: string
                        mode:
                          description: Type of access to the disk resource. By default,
                            a disk is attached in READ_WRITE mode.
                          type: string
                      type: object
                    type: array
                  serviceAccountId:
                    description: ID of the service account authorized for this instance.
                    type: string
                  status:
                    description: The status of this instance.
                    type: string
                  zone:
                    description: The availability zone where the virtual machine will
                      be created. If it is not provided, the default provider folder
                      is used.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}

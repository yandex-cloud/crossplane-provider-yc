---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: buckets.storage.yandex-cloud.jet.crossplane.io
spec:
  group: storage.yandex-cloud.jet.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - yandex-cloud
    kind: Bucket
    listKind: BucketList
    plural: buckets
    singular: bucket
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Bucket is the Schema for the Buckets API. Allows management of
          a Yandex.Cloud Storage Bucket.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BucketSpec defines the desired state of Bucket
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  accessKey:
                    description: |-
                      The access key to use when applying changes. If omitted, storage_access_key specified in
                      provider config (explicitly or within shared_credentials_file) is used.
                    type: string
                  accessKeyRef:
                    description: Reference to a ServiceAccountStaticAccessKey in iam
                      to populate accessKey.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  accessKeySelector:
                    description: Selector for a ServiceAccountStaticAccessKey in iam
                      to populate accessKey.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  acl:
                    description: |-
                      The predefined ACL to apply.
                      Defaults to private. Conflicts with grant.
                    type: string
                  anonymousAccessFlags:
                    description: |-
                      Provides various access to objects.
                      See bucket availability
                      for more infomation.
                    items:
                      properties:
                        configRead:
                          description: Allows to read objects in bucket anonymously.
                          type: boolean
                        list:
                          description: Allows to list object in bucket anonymously.
                          type: boolean
                        read:
                          description: Allows to read objects in bucket anonymously.
                          type: boolean
                      type: object
                    type: array
                  bucket:
                    description: The name of the bucket.
                    type: string
                  bucketPrefix:
                    description: |-
                      Creates a unique bucket name beginning with the specified prefix.
                      Conflicts with bucket.
                    type: string
                  corsRule:
                    description: A rule of Cross-Origin Resource Sharing (documented
                      below).
                    items:
                      properties:
                        allowedHeaders:
                          description: Specifies which headers are allowed.
                          items:
                            type: string
                          type: array
                        allowedMethods:
                          description: Specifies which methods are allowed. Can be
                            GET, PUT, POST, DELETE or HEAD.
                          items:
                            type: string
                          type: array
                        allowedOrigins:
                          description: Specifies which origins are allowed.
                          items:
                            type: string
                          type: array
                        exposeHeaders:
                          description: Specifies expose header in the response.
                          items:
                            type: string
                          type: array
                        maxAgeSeconds:
                          description: Specifies time in seconds that browser can
                            cache the response for a preflight request.
                          type: number
                      type: object
                    type: array
                  defaultStorageClass:
                    description: |-
                      Storage class which is used for storing objects by default.
                      Available values are: "STANDARD", "COLD", "ICE". Default is "STANDARD".
                      See storage class for more inforamtion.
                    type: string
                  folderId:
                    description: Allow to create bucket in different folder.
                    type: string
                  folderIdRef:
                    description: Reference to a Folder in resourcemanager to populate
                      folderId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  folderIdSelector:
                    description: Selector for a Folder in resourcemanager to populate
                      folderId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  forceDestroy:
                    description: A boolean that indicates all objects should be deleted
                      from the bucket so that the bucket can be destroyed without
                      error. These objects are not recoverable.
                    type: boolean
                  grant:
                    description: An ACL policy grant. Conflicts with acl.
                    items:
                      properties:
                        id:
                          description: Canonical user id to grant for. Used only when
                            type is CanonicalUser.
                          type: string
                        permissions:
                          description: List of permissions to apply for grantee. Valid
                            values are READ, WRITE, FULL_CONTROL.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                        type:
                          description: Type of grantee to apply for. Valid values
                            are CanonicalUser and Group.
                          type: string
                        uri:
                          description: Uri address to grant for. Used only when type
                            is Group.
                          type: string
                      type: object
                    type: array
                  https:
                    description: Manages https certificates for bucket. See https
                      for more infomation.
                    items:
                      properties:
                        certificateId:
                          description: — Id of the certificate in Certificate Manager,
                            that will be used for bucket.
                          type: string
                      type: object
                    type: array
                  lifecycleRule:
                    description: A configuration of object lifecycle management (documented
                      below).
                    items:
                      properties:
                        abortIncompleteMultipartUploadDays:
                          description: Specifies the number of days after initiating
                            a multipart upload when the multipart upload must be completed.
                          type: number
                        enabled:
                          description: Enable versioning. Once you version-enable
                            a bucket, it can never return to an unversioned state.
                            You can, however, suspend versioning on that bucket.
                          type: boolean
                        expiration:
                          description: Specifies a period in the object's expire (documented
                            below).
                          items:
                            properties:
                              date:
                                description: Specifies the date after which you want
                                  the corresponding action to take effect.
                                type: string
                              days:
                                description: Specifies a retention period in days
                                  after uploading an object version. It must be a
                                  positive integer. You can't set it simultaneously
                                  with years.
                                type: number
                              expiredObjectDeleteMarker:
                                description: On a versioned bucket (versioning-enabled
                                  or versioning-suspended bucket), you can add this
                                  element in the lifecycle configuration to direct
                                  Object Storage to delete expired object delete markers.
                                type: boolean
                            type: object
                          type: array
                        filter:
                          description: 'Filter block identifies one or more objects
                            to which the rule applies. A Filter must have exactly
                            one of Prefix, Tag, or And specified. The filter supports
                            the following options:'
                          items:
                            properties:
                              and:
                                description: 'operator applied to one or more filter
                                  parameters. It should be used when both prefix and
                                  tags are used. It supports the following parameters:'
                                items:
                                  properties:
                                    objectSizeGreaterThan:
                                      type: number
                                    objectSizeLessThan:
                                      type: number
                                    prefix:
                                      description: Object key prefix identifying one
                                        or more objects to which the rule applies.
                                      type: string
                                    tags:
                                      additionalProperties:
                                        type: string
                                      type: object
                                      x-kubernetes-map-type: granular
                                  type: object
                                type: array
                              objectSizeGreaterThan:
                                type: number
                              objectSizeLessThan:
                                type: number
                              prefix:
                                description: Object key prefix identifying one or
                                  more objects to which the rule applies.
                                type: string
                              tag:
                                items:
                                  properties:
                                    key:
                                      type: string
                                    value:
                                      type: string
                                  type: object
                                type: array
                            type: object
                          type: array
                        id:
                          description: Canonical user id to grant for. Used only when
                            type is CanonicalUser.
                          type: string
                        noncurrentVersionExpiration:
                          description: Specifies when noncurrent object versions expire
                            (documented below).
                          items:
                            properties:
                              days:
                                description: Specifies a retention period in days
                                  after uploading an object version. It must be a
                                  positive integer. You can't set it simultaneously
                                  with years.
                                type: number
                            type: object
                          type: array
                        noncurrentVersionTransition:
                          description: Specifies when noncurrent object versions transitions
                            (documented below).
                          items:
                            properties:
                              days:
                                description: Specifies a retention period in days
                                  after uploading an object version. It must be a
                                  positive integer. You can't set it simultaneously
                                  with years.
                                type: number
                              storageClass:
                                description: 'Specifies the storage class to which
                                  you want the object to transition. Supported values:
                                  [STANDARD_IA, COLD, ICE].'
                                type: string
                            type: object
                          type: array
                        prefix:
                          description: Object key prefix identifying one or more objects
                            to which the rule applies.
                          type: string
                        transition:
                          description: Specifies a period in the object's transitions
                            (documented below).
                          items:
                            properties:
                              date:
                                description: Specifies the date after which you want
                                  the corresponding action to take effect.
                                type: string
                              days:
                                description: Specifies a retention period in days
                                  after uploading an object version. It must be a
                                  positive integer. You can't set it simultaneously
                                  with years.
                                type: number
                              storageClass:
                                description: 'Specifies the storage class to which
                                  you want the object to transition. Supported values:
                                  [STANDARD_IA, COLD, ICE].'
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  logging:
                    description: A settings of bucket logging (documented below).
                    items:
                      properties:
                        targetBucket:
                          description: The name of the bucket that will receive the
                            log objects.
                          type: string
                        targetPrefix:
                          description: To specify a key prefix for log objects.
                          type: string
                      type: object
                    type: array
                  maxSize:
                    description: The size of bucket, in bytes. See size limiting for
                      more information.
                    type: number
                  objectLockConfiguration:
                    description: A configuration of object lock management (documented
                      below).
                    items:
                      properties:
                        objectLockEnabled:
                          description: Enable object locking in a bucket. Require
                            versioning to be enabled.
                          type: string
                        rule:
                          description: Specifies a default locking configuration for
                            added objects. Require object_lock_enabled to be enabled.
                          items:
                            properties:
                              defaultRetention:
                                items:
                                  properties:
                                    days:
                                      description: Specifies a retention period in
                                        days after uploading an object version. It
                                        must be a positive integer. You can't set
                                        it simultaneously with years.
                                      type: number
                                    mode:
                                      description: Specifies a type of object lock.
                                        One of ["GOVERNANCE", "COMPLIANCE"].
                                      type: string
                                    years:
                                      description: Specifies a retention period in
                                        years after uploading an object version. It
                                        must be a positive integer. You can't set
                                        it simultaneously with days.
                                      type: number
                                  type: object
                                type: array
                            type: object
                          type: array
                      type: object
                    type: array
                  policy:
                    type: string
                  secretKeySecretRef:
                    description: |-
                      The secret key to use when applying changes. If omitted, storage_secret_key specified in
                      provider config (explicitly or within shared_credentials_file) is used.
                    properties:
                      key:
                        description: The key to select.
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                      namespace:
                        description: Namespace of the secret.
                        type: string
                    required:
                    - key
                    - name
                    - namespace
                    type: object
                  serverSideEncryptionConfiguration:
                    description: A configuration of server-side encryption for the
                      bucket (documented below)
                    items:
                      properties:
                        rule:
                          description: Specifies a default locking configuration for
                            added objects. Require object_lock_enabled to be enabled.
                          items:
                            properties:
                              applyServerSideEncryptionByDefault:
                                description: A single object for setting server-side
                                  encryption by default. (documented below)
                                items:
                                  properties:
                                    kmsMasterKeyId:
                                      description: The KMS master key ID used for
                                        the SSE-KMS encryption.
                                      type: string
                                    sseAlgorithm:
                                      description: The server-side encryption algorithm
                                        to use. Single valid value is aws:kms
                                      type: string
                                  type: object
                                type: array
                            type: object
                          type: array
                      type: object
                    type: array
                  tags:
                    additionalProperties:
                      type: string
                    type: object
                    x-kubernetes-map-type: granular
                  versioning:
                    description: A state of versioning (documented below)
                    items:
                      properties:
                        enabled:
                          description: Enable versioning. Once you version-enable
                            a bucket, it can never return to an unversioned state.
                            You can, however, suspend versioning on that bucket.
                          type: boolean
                      type: object
                    type: array
                  website:
                    description: A website object (documented below).
                    items:
                      properties:
                        errorDocument:
                          description: An absolute path to the document to return
                            in case of a 4XX error.
                          type: string
                        indexDocument:
                          description: Storage returns this index document when requests
                            are made to the root domain or any of the subfolders.
                          type: string
                        redirectAllRequestsTo:
                          description: A hostname to redirect all website requests
                            for this bucket to. Hostname can optionally be prefixed
                            with a protocol (http:// or https://) to use when redirecting
                            requests. The default is the protocol that is used in
                            the original request.
                          type: string
                        routingRules:
                          description: A json array containing routing rules describing
                            redirect behavior and when redirects are applied.
                          type: string
                      type: object
                    type: array
                  websiteDomain:
                    description: The domain of the website endpoint, if the bucket
                      is configured with a website. If not, this will be an empty
                      string.
                    type: string
                  websiteEndpoint:
                    description: The website endpoint, if the bucket is configured
                      with a website. If not, this will be an empty string.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  accessKey:
                    description: |-
                      The access key to use when applying changes. If omitted, storage_access_key specified in
                      provider config (explicitly or within shared_credentials_file) is used.
                    type: string
                  accessKeyRef:
                    description: Reference to a ServiceAccountStaticAccessKey in iam
                      to populate accessKey.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  accessKeySelector:
                    description: Selector for a ServiceAccountStaticAccessKey in iam
                      to populate accessKey.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  acl:
                    description: |-
                      The predefined ACL to apply.
                      Defaults to private. Conflicts with grant.
                    type: string
                  anonymousAccessFlags:
                    description: |-
                      Provides various access to objects.
                      See bucket availability
                      for more infomation.
                    items:
                      properties:
                        configRead:
                          description: Allows to read objects in bucket anonymously.
                          type: boolean
                        list:
                          description: Allows to list object in bucket anonymously.
                          type: boolean
                        read:
                          description: Allows to read objects in bucket anonymously.
                          type: boolean
                      type: object
                    type: array
                  bucket:
                    description: The name of the bucket.
                    type: string
                  bucketPrefix:
                    description: |-
                      Creates a unique bucket name beginning with the specified prefix.
                      Conflicts with bucket.
                    type: string
                  corsRule:
                    description: A rule of Cross-Origin Resource Sharing (documented
                      below).
                    items:
                      properties:
                        allowedHeaders:
                          description: Specifies which headers are allowed.
                          items:
                            type: string
                          type: array
                        allowedMethods:
                          description: Specifies which methods are allowed. Can be
                            GET, PUT, POST, DELETE or HEAD.
                          items:
                            type: string
                          type: array
                        allowedOrigins:
                          description: Specifies which origins are allowed.
                          items:
                            type: string
                          type: array
                        exposeHeaders:
                          description: Specifies expose header in the response.
                          items:
                            type: string
                          type: array
                        maxAgeSeconds:
                          description: Specifies time in seconds that browser can
                            cache the response for a preflight request.
                          type: number
                      type: object
                    type: array
                  defaultStorageClass:
                    description: |-
                      Storage class which is used for storing objects by default.
                      Available values are: "STANDARD", "COLD", "ICE". Default is "STANDARD".
                      See storage class for more inforamtion.
                    type: string
                  folderId:
                    description: Allow to create bucket in different folder.
                    type: string
                  folderIdRef:
                    description: Reference to a Folder in resourcemanager to populate
                      folderId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  folderIdSelector:
                    description: Selector for a Folder in resourcemanager to populate
                      folderId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  forceDestroy:
                    description: A boolean that indicates all objects should be deleted
                      from the bucket so that the bucket can be destroyed without
                      error. These objects are not recoverable.
                    type: boolean
                  grant:
                    description: An ACL policy grant. Conflicts with acl.
                    items:
                      properties:
                        id:
                          description: Canonical user id to grant for. Used only when
                            type is CanonicalUser.
                          type: string
                        permissions:
                          description: List of permissions to apply for grantee. Valid
                            values are READ, WRITE, FULL_CONTROL.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                        type:
                          description: Type of grantee to apply for. Valid values
                            are CanonicalUser and Group.
                          type: string
                        uri:
                          description: Uri address to grant for. Used only when type
                            is Group.
                          type: string
                      type: object
                    type: array
                  https:
                    description: Manages https certificates for bucket. See https
                      for more infomation.
                    items:
                      properties:
                        certificateId:
                          description: — Id of the certificate in Certificate Manager,
                            that will be used for bucket.
                          type: string
                      type: object
                    type: array
                  lifecycleRule:
                    description: A configuration of object lifecycle management (documented
                      below).
                    items:
                      properties:
                        abortIncompleteMultipartUploadDays:
                          description: Specifies the number of days after initiating
                            a multipart upload when the multipart upload must be completed.
                          type: number
                        enabled:
                          description: Enable versioning. Once you version-enable
                            a bucket, it can never return to an unversioned state.
                            You can, however, suspend versioning on that bucket.
                          type: boolean
                        expiration:
                          description: Specifies a period in the object's expire (documented
                            below).
                          items:
                            properties:
                              date:
                                description: Specifies the date after which you want
                                  the corresponding action to take effect.
                                type: string
                              days:
                                description: Specifies a retention period in days
                                  after uploading an object version. It must be a
                                  positive integer. You can't set it simultaneously
                                  with years.
                                type: number
                              expiredObjectDeleteMarker:
                                description: On a versioned bucket (versioning-enabled
                                  or versioning-suspended bucket), you can add this
                                  element in the lifecycle configuration to direct
                                  Object Storage to delete expired object delete markers.
                                type: boolean
                            type: object
                          type: array
                        filter:
                          description: 'Filter block identifies one or more objects
                            to which the rule applies. A Filter must have exactly
                            one of Prefix, Tag, or And specified. The filter supports
                            the following options:'
                          items:
                            properties:
                              and:
                                description: 'operator applied to one or more filter
                                  parameters. It should be used when both prefix and
                                  tags are used. It supports the following parameters:'
                                items:
                                  properties:
                                    objectSizeGreaterThan:
                                      type: number
                                    objectSizeLessThan:
                                      type: number
                                    prefix:
                                      description: Object key prefix identifying one
                                        or more objects to which the rule applies.
                                      type: string
                                    tags:
                                      additionalProperties:
                                        type: string
                                      type: object
                                      x-kubernetes-map-type: granular
                                  type: object
                                type: array
                              objectSizeGreaterThan:
                                type: number
                              objectSizeLessThan:
                                type: number
                              prefix:
                                description: Object key prefix identifying one or
                                  more objects to which the rule applies.
                                type: string
                              tag:
                                items:
                                  properties:
                                    key:
                                      type: string
                                    value:
                                      type: string
                                  type: object
                                type: array
                            type: object
                          type: array
                        id:
                          description: Canonical user id to grant for. Used only when
                            type is CanonicalUser.
                          type: string
                        noncurrentVersionExpiration:
                          description: Specifies when noncurrent object versions expire
                            (documented below).
                          items:
                            properties:
                              days:
                                description: Specifies a retention period in days
                                  after uploading an object version. It must be a
                                  positive integer. You can't set it simultaneously
                                  with years.
                                type: number
                            type: object
                          type: array
                        noncurrentVersionTransition:
                          description: Specifies when noncurrent object versions transitions
                            (documented below).
                          items:
                            properties:
                              days:
                                description: Specifies a retention period in days
                                  after uploading an object version. It must be a
                                  positive integer. You can't set it simultaneously
                                  with years.
                                type: number
                              storageClass:
                                description: 'Specifies the storage class to which
                                  you want the object to transition. Supported values:
                                  [STANDARD_IA, COLD, ICE].'
                                type: string
                            type: object
                          type: array
                        prefix:
                          description: Object key prefix identifying one or more objects
                            to which the rule applies.
                          type: string
                        transition:
                          description: Specifies a period in the object's transitions
                            (documented below).
                          items:
                            properties:
                              date:
                                description: Specifies the date after which you want
                                  the corresponding action to take effect.
                                type: string
                              days:
                                description: Specifies a retention period in days
                                  after uploading an object version. It must be a
                                  positive integer. You can't set it simultaneously
                                  with years.
                                type: number
                              storageClass:
                                description: 'Specifies the storage class to which
                                  you want the object to transition. Supported values:
                                  [STANDARD_IA, COLD, ICE].'
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  logging:
                    description: A settings of bucket logging (documented below).
                    items:
                      properties:
                        targetBucket:
                          description: The name of the bucket that will receive the
                            log objects.
                          type: string
                        targetPrefix:
                          description: To specify a key prefix for log objects.
                          type: string
                      type: object
                    type: array
                  maxSize:
                    description: The size of bucket, in bytes. See size limiting for
                      more information.
                    type: number
                  objectLockConfiguration:
                    description: A configuration of object lock management (documented
                      below).
                    items:
                      properties:
                        objectLockEnabled:
                          description: Enable object locking in a bucket. Require
                            versioning to be enabled.
                          type: string
                        rule:
                          description: Specifies a default locking configuration for
                            added objects. Require object_lock_enabled to be enabled.
                          items:
                            properties:
                              defaultRetention:
                                items:
                                  properties:
                                    days:
                                      description: Specifies a retention period in
                                        days after uploading an object version. It
                                        must be a positive integer. You can't set
                                        it simultaneously with years.
                                      type: number
                                    mode:
                                      description: Specifies a type of object lock.
                                        One of ["GOVERNANCE", "COMPLIANCE"].
                                      type: string
                                    years:
                                      description: Specifies a retention period in
                                        years after uploading an object version. It
                                        must be a positive integer. You can't set
                                        it simultaneously with days.
                                      type: number
                                  type: object
                                type: array
                            type: object
                          type: array
                      type: object
                    type: array
                  policy:
                    type: string
                  serverSideEncryptionConfiguration:
                    description: A configuration of server-side encryption for the
                      bucket (documented below)
                    items:
                      properties:
                        rule:
                          description: Specifies a default locking configuration for
                            added objects. Require object_lock_enabled to be enabled.
                          items:
                            properties:
                              applyServerSideEncryptionByDefault:
                                description: A single object for setting server-side
                                  encryption by default. (documented below)
                                items:
                                  properties:
                                    kmsMasterKeyId:
                                      description: The KMS master key ID used for
                                        the SSE-KMS encryption.
                                      type: string
                                    sseAlgorithm:
                                      description: The server-side encryption algorithm
                                        to use. Single valid value is aws:kms
                                      type: string
                                  type: object
                                type: array
                            type: object
                          type: array
                      type: object
                    type: array
                  tags:
                    additionalProperties:
                      type: string
                    type: object
                    x-kubernetes-map-type: granular
                  versioning:
                    description: A state of versioning (documented below)
                    items:
                      properties:
                        enabled:
                          description: Enable versioning. Once you version-enable
                            a bucket, it can never return to an unversioned state.
                            You can, however, suspend versioning on that bucket.
                          type: boolean
                      type: object
                    type: array
                  website:
                    description: A website object (documented below).
                    items:
                      properties:
                        errorDocument:
                          description: An absolute path to the document to return
                            in case of a 4XX error.
                          type: string
                        indexDocument:
                          description: Storage returns this index document when requests
                            are made to the root domain or any of the subfolders.
                          type: string
                        redirectAllRequestsTo:
                          description: A hostname to redirect all website requests
                            for this bucket to. Hostname can optionally be prefixed
                            with a protocol (http:// or https://) to use when redirecting
                            requests. The default is the protocol that is used in
                            the original request.
                          type: string
                        routingRules:
                          description: A json array containing routing rules describing
                            redirect behavior and when redirects are applied.
                          type: string
                      type: object
                    type: array
                  websiteDomain:
                    description: The domain of the website endpoint, if the bucket
                      is configured with a website. If not, this will be an empty
                      string.
                    type: string
                  websiteEndpoint:
                    description: The website endpoint, if the bucket is configured
                      with a website. If not, this will be an empty string.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              publishConnectionDetailsTo:
                description: |-
                  PublishConnectionDetailsTo specifies the connection secret config which
                  contains a name, metadata and a reference to secret store config to
                  which any connection details for this managed resource should be written.
                  Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  configRef:
                    default:
                      name: default
                    description: |-
                      SecretStoreConfigRef specifies which secret store config should be used
                      for this ConnectionSecret.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  metadata:
                    description: Metadata is the metadata for connection secret.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: |-
                          Annotations are the annotations to be added to connection secret.
                          - For Kubernetes secrets, this will be used as "metadata.annotations".
                          - It is up to Secret Store implementation for others store types.
                        type: object
                      labels:
                        additionalProperties:
                          type: string
                        description: |-
                          Labels are the labels/tags to be added to connection secret.
                          - For Kubernetes secrets, this will be used as "metadata.labels".
                          - It is up to Secret Store implementation for others store types.
                        type: object
                      type:
                        description: |-
                          Type is the SecretType for the connection secret.
                          - Only valid for Kubernetes Secret Stores.
                        type: string
                    type: object
                  name:
                    description: Name is the name of the connection secret.
                    type: string
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                  This field is planned to be replaced in a future release in favor of
                  PublishConnectionDetailsTo. Currently, both could be set independently
                  and connection details would be published to both without affecting
                  each other.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
          status:
            description: BucketStatus defines the observed state of Bucket.
            properties:
              atProvider:
                properties:
                  accessKey:
                    description: |-
                      The access key to use when applying changes. If omitted, storage_access_key specified in
                      provider config (explicitly or within shared_credentials_file) is used.
                    type: string
                  acl:
                    description: |-
                      The predefined ACL to apply.
                      Defaults to private. Conflicts with grant.
                    type: string
                  anonymousAccessFlags:
                    description: |-
                      Provides various access to objects.
                      See bucket availability
                      for more infomation.
                    items:
                      properties:
                        configRead:
                          description: Allows to read objects in bucket anonymously.
                          type: boolean
                        list:
                          description: Allows to list object in bucket anonymously.
                          type: boolean
                        read:
                          description: Allows to read objects in bucket anonymously.
                          type: boolean
                      type: object
                    type: array
                  bucket:
                    description: The name of the bucket.
                    type: string
                  bucketDomainName:
                    description: The bucket domain name.
                    type: string
                  bucketPrefix:
                    description: |-
                      Creates a unique bucket name beginning with the specified prefix.
                      Conflicts with bucket.
                    type: string
                  corsRule:
                    description: A rule of Cross-Origin Resource Sharing (documented
                      below).
                    items:
                      properties:
                        allowedHeaders:
                          description: Specifies which headers are allowed.
                          items:
                            type: string
                          type: array
                        allowedMethods:
                          description: Specifies which methods are allowed. Can be
                            GET, PUT, POST, DELETE or HEAD.
                          items:
                            type: string
                          type: array
                        allowedOrigins:
                          description: Specifies which origins are allowed.
                          items:
                            type: string
                          type: array
                        exposeHeaders:
                          description: Specifies expose header in the response.
                          items:
                            type: string
                          type: array
                        maxAgeSeconds:
                          description: Specifies time in seconds that browser can
                            cache the response for a preflight request.
                          type: number
                      type: object
                    type: array
                  defaultStorageClass:
                    description: |-
                      Storage class which is used for storing objects by default.
                      Available values are: "STANDARD", "COLD", "ICE". Default is "STANDARD".
                      See storage class for more inforamtion.
                    type: string
                  folderId:
                    description: Allow to create bucket in different folder.
                    type: string
                  forceDestroy:
                    description: A boolean that indicates all objects should be deleted
                      from the bucket so that the bucket can be destroyed without
                      error. These objects are not recoverable.
                    type: boolean
                  grant:
                    description: An ACL policy grant. Conflicts with acl.
                    items:
                      properties:
                        id:
                          description: Canonical user id to grant for. Used only when
                            type is CanonicalUser.
                          type: string
                        permissions:
                          description: List of permissions to apply for grantee. Valid
                            values are READ, WRITE, FULL_CONTROL.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                        type:
                          description: Type of grantee to apply for. Valid values
                            are CanonicalUser and Group.
                          type: string
                        uri:
                          description: Uri address to grant for. Used only when type
                            is Group.
                          type: string
                      type: object
                    type: array
                  https:
                    description: Manages https certificates for bucket. See https
                      for more infomation.
                    items:
                      properties:
                        certificateId:
                          description: — Id of the certificate in Certificate Manager,
                            that will be used for bucket.
                          type: string
                      type: object
                    type: array
                  id:
                    description: Canonical user id to grant for. Used only when type
                      is CanonicalUser.
                    type: string
                  lifecycleRule:
                    description: A configuration of object lifecycle management (documented
                      below).
                    items:
                      properties:
                        abortIncompleteMultipartUploadDays:
                          description: Specifies the number of days after initiating
                            a multipart upload when the multipart upload must be completed.
                          type: number
                        enabled:
                          description: Enable versioning. Once you version-enable
                            a bucket, it can never return to an unversioned state.
                            You can, however, suspend versioning on that bucket.
                          type: boolean
                        expiration:
                          description: Specifies a period in the object's expire (documented
                            below).
                          items:
                            properties:
                              date:
                                description: Specifies the date after which you want
                                  the corresponding action to take effect.
                                type: string
                              days:
                                description: Specifies a retention period in days
                                  after uploading an object version. It must be a
                                  positive integer. You can't set it simultaneously
                                  with years.
                                type: number
                              expiredObjectDeleteMarker:
                                description: On a versioned bucket (versioning-enabled
                                  or versioning-suspended bucket), you can add this
                                  element in the lifecycle configuration to direct
                                  Object Storage to delete expired object delete markers.
                                type: boolean
                            type: object
                          type: array
                        filter:
                          description: 'Filter block identifies one or more objects
                            to which the rule applies. A Filter must have exactly
                            one of Prefix, Tag, or And specified. The filter supports
                            the following options:'
                          items:
                            properties:
                              and:
                                description: 'operator applied to one or more filter
                                  parameters. It should be used when both prefix and
                                  tags are used. It supports the following parameters:'
                                items:
                                  properties:
                                    objectSizeGreaterThan:
                                      type: number
                                    objectSizeLessThan:
                                      type: number
                                    prefix:
                                      description: Object key prefix identifying one
                                        or more objects to which the rule applies.
                                      type: string
                                    tags:
                                      additionalProperties:
                                        type: string
                                      type: object
                                      x-kubernetes-map-type: granular
                                  type: object
                                type: array
                              objectSizeGreaterThan:
                                type: number
                              objectSizeLessThan:
                                type: number
                              prefix:
                                description: Object key prefix identifying one or
                                  more objects to which the rule applies.
                                type: string
                              tag:
                                items:
                                  properties:
                                    key:
                                      type: string
                                    value:
                                      type: string
                                  type: object
                                type: array
                            type: object
                          type: array
                        id:
                          description: Canonical user id to grant for. Used only when
                            type is CanonicalUser.
                          type: string
                        noncurrentVersionExpiration:
                          description: Specifies when noncurrent object versions expire
                            (documented below).
                          items:
                            properties:
                              days:
                                description: Specifies a retention period in days
                                  after uploading an object version. It must be a
                                  positive integer. You can't set it simultaneously
                                  with years.
                                type: number
                            type: object
                          type: array
                        noncurrentVersionTransition:
                          description: Specifies when noncurrent object versions transitions
                            (documented below).
                          items:
                            properties:
                              days:
                                description: Specifies a retention period in days
                                  after uploading an object version. It must be a
                                  positive integer. You can't set it simultaneously
                                  with years.
                                type: number
                              storageClass:
                                description: 'Specifies the storage class to which
                                  you want the object to transition. Supported values:
                                  [STANDARD_IA, COLD, ICE].'
                                type: string
                            type: object
                          type: array
                        prefix:
                          description: Object key prefix identifying one or more objects
                            to which the rule applies.
                          type: string
                        transition:
                          description: Specifies a period in the object's transitions
                            (documented below).
                          items:
                            properties:
                              date:
                                description: Specifies the date after which you want
                                  the corresponding action to take effect.
                                type: string
                              days:
                                description: Specifies a retention period in days
                                  after uploading an object version. It must be a
                                  positive integer. You can't set it simultaneously
                                  with years.
                                type: number
                              storageClass:
                                description: 'Specifies the storage class to which
                                  you want the object to transition. Supported values:
                                  [STANDARD_IA, COLD, ICE].'
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  logging:
                    description: A settings of bucket logging (documented below).
                    items:
                      properties:
                        targetBucket:
                          description: The name of the bucket that will receive the
                            log objects.
                          type: string
                        targetPrefix:
                          description: To specify a key prefix for log objects.
                          type: string
                      type: object
                    type: array
                  maxSize:
                    description: The size of bucket, in bytes. See size limiting for
                      more information.
                    type: number
                  objectLockConfiguration:
                    description: A configuration of object lock management (documented
                      below).
                    items:
                      properties:
                        objectLockEnabled:
                          description: Enable object locking in a bucket. Require
                            versioning to be enabled.
                          type: string
                        rule:
                          description: Specifies a default locking configuration for
                            added objects. Require object_lock_enabled to be enabled.
                          items:
                            properties:
                              defaultRetention:
                                items:
                                  properties:
                                    days:
                                      description: Specifies a retention period in
                                        days after uploading an object version. It
                                        must be a positive integer. You can't set
                                        it simultaneously with years.
                                      type: number
                                    mode:
                                      description: Specifies a type of object lock.
                                        One of ["GOVERNANCE", "COMPLIANCE"].
                                      type: string
                                    years:
                                      description: Specifies a retention period in
                                        years after uploading an object version. It
                                        must be a positive integer. You can't set
                                        it simultaneously with days.
                                      type: number
                                  type: object
                                type: array
                            type: object
                          type: array
                      type: object
                    type: array
                  policy:
                    type: string
                  serverSideEncryptionConfiguration:
                    description: A configuration of server-side encryption for the
                      bucket (documented below)
                    items:
                      properties:
                        rule:
                          description: Specifies a default locking configuration for
                            added objects. Require object_lock_enabled to be enabled.
                          items:
                            properties:
                              applyServerSideEncryptionByDefault:
                                description: A single object for setting server-side
                                  encryption by default. (documented below)
                                items:
                                  properties:
                                    kmsMasterKeyId:
                                      description: The KMS master key ID used for
                                        the SSE-KMS encryption.
                                      type: string
                                    sseAlgorithm:
                                      description: The server-side encryption algorithm
                                        to use. Single valid value is aws:kms
                                      type: string
                                  type: object
                                type: array
                            type: object
                          type: array
                      type: object
                    type: array
                  tags:
                    additionalProperties:
                      type: string
                    type: object
                    x-kubernetes-map-type: granular
                  versioning:
                    description: A state of versioning (documented below)
                    items:
                      properties:
                        enabled:
                          description: Enable versioning. Once you version-enable
                            a bucket, it can never return to an unversioned state.
                            You can, however, suspend versioning on that bucket.
                          type: boolean
                      type: object
                    type: array
                  website:
                    description: A website object (documented below).
                    items:
                      properties:
                        errorDocument:
                          description: An absolute path to the document to return
                            in case of a 4XX error.
                          type: string
                        indexDocument:
                          description: Storage returns this index document when requests
                            are made to the root domain or any of the subfolders.
                          type: string
                        redirectAllRequestsTo:
                          description: A hostname to redirect all website requests
                            for this bucket to. Hostname can optionally be prefixed
                            with a protocol (http:// or https://) to use when redirecting
                            requests. The default is the protocol that is used in
                            the original request.
                          type: string
                        routingRules:
                          description: A json array containing routing rules describing
                            redirect behavior and when redirects are applied.
                          type: string
                      type: object
                    type: array
                  websiteDomain:
                    description: The domain of the website endpoint, if the bucket
                      is configured with a website. If not, this will be an empty
                      string.
                    type: string
                  websiteEndpoint:
                    description: The website endpoint, if the bucket is configured
                      with a website. If not, this will be an empty string.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}

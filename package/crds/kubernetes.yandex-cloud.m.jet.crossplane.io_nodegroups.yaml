---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: nodegroups.kubernetes.yandex-cloud.m.jet.crossplane.io
spec:
  group: kubernetes.yandex-cloud.m.jet.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - yandex-cloud
    kind: NodeGroup
    listKind: NodeGroupList
    plural: nodegroups
    singular: nodegroup
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: NodeGroup is the Schema for the NodeGroups API. Manages the yandex_kubernetes_node_group
          resource.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: NodeGroupSpec defines the desired state of NodeGroup
            properties:
              forProvider:
                properties:
                  allocationPolicy:
                    description: |-
                      [Block]. This argument specify subnets (zones), that will be used by node group compute instances.
                      This argument specify subnets (zones), that will be used by node group compute instances.
                    items:
                      properties:
                        location:
                          description: |-
                            [Block]. Repeated field, that specify subnets (zones), that will be used by node group compute instances. Subnet specified by subnet_id should be allocated in zone specified by 'zone' argument.
                            Repeated field, that specify subnets (zones), that will be used by node group compute instances. Subnet specified by `subnet_id` should be allocated in zone specified by 'zone' argument.
                          items:
                            properties:
                              subnetId:
                                description: |-
                                  (String). ID of the subnet, that will be used by one compute instance in node group.
                                  ID of the subnet, that will be used by one compute instance in node group.
                                type: string
                              subnetIdRef:
                                description: Reference to a Subnet in vpc to populate
                                  subnetId.
                                properties:
                                  name:
                                    description: Name of the referenced object.
                                    type: string
                                  namespace:
                                    description: Namespace of the referenced object
                                    type: string
                                  policy:
                                    description: Policies for referencing.
                                    properties:
                                      resolution:
                                        default: Required
                                        description: |-
                                          Resolution specifies whether resolution of this reference is required.
                                          The default is 'Required', which means the reconcile will fail if the
                                          reference cannot be resolved. 'Optional' means this reference will be
                                          a no-op if it cannot be resolved.
                                        enum:
                                        - Required
                                        - Optional
                                        type: string
                                      resolve:
                                        description: |-
                                          Resolve specifies when this reference should be resolved. The default
                                          is 'IfNotPresent', which will attempt to resolve the reference only when
                                          the corresponding field is not present. Use 'Always' to resolve the
                                          reference on every reconcile.
                                        enum:
                                        - Always
                                        - IfNotPresent
                                        type: string
                                    type: object
                                required:
                                - name
                                type: object
                              subnetIdSelector:
                                description: Selector for a Subnet in vpc to populate
                                  subnetId.
                                properties:
                                  matchControllerRef:
                                    description: |-
                                      MatchControllerRef ensures an object with the same controller reference
                                      as the selecting object is selected.
                                    type: boolean
                                  matchLabels:
                                    additionalProperties:
                                      type: string
                                    description: MatchLabels ensures an object with
                                      matching labels is selected.
                                    type: object
                                  namespace:
                                    description: Namespace for the selector
                                    type: string
                                  policy:
                                    description: Policies for selection.
                                    properties:
                                      resolution:
                                        default: Required
                                        description: |-
                                          Resolution specifies whether resolution of this reference is required.
                                          The default is 'Required', which means the reconcile will fail if the
                                          reference cannot be resolved. 'Optional' means this reference will be
                                          a no-op if it cannot be resolved.
                                        enum:
                                        - Required
                                        - Optional
                                        type: string
                                      resolve:
                                        description: |-
                                          Resolve specifies when this reference should be resolved. The default
                                          is 'IfNotPresent', which will attempt to resolve the reference only when
                                          the corresponding field is not present. Use 'Always' to resolve the
                                          reference on every reconcile.
                                        enum:
                                        - Always
                                        - IfNotPresent
                                        type: string
                                    type: object
                                type: object
                              zone:
                                description: |-
                                  (String). ID of the availability zone where for one compute instance in node group.
                                  ID of the availability zone where for one compute instance in node group.
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  allowedUnsafeSysctls:
                    description: |-
                      (List Of String). A list of allowed unsafe sysctl parameters for this node group. For more details see documentation.
                      A list of allowed unsafe `sysctl` parameters for this node group. For more details see [documentation](https://kubernetes.io/docs/tasks/administer-cluster/sysctl-cluster).
                    items:
                      type: string
                    type: array
                  clusterId:
                    description: |-
                      (String). The ID of the Kubernetes cluster that this node group belongs to.
                      The ID of the Kubernetes cluster that this node group belongs to.
                    type: string
                  clusterIdRef:
                    description: Reference to a Cluster to populate clusterId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  clusterIdSelector:
                    description: Selector for a Cluster to populate clusterId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  deployPolicy:
                    description: |-
                      [Block]. Deploy policy of the node group.
                      Deploy policy of the node group.
                    items:
                      properties:
                        maxExpansion:
                          description: |-
                            (Number). The maximum number of instances that can be temporarily allocated above the group's target size during the update.
                            The maximum number of instances that can be temporarily allocated above the group's target size during the update.
                          type: number
                        maxUnavailable:
                          description: |-
                            (Number). The maximum number of running instances that can be taken offline during update.
                            The maximum number of running instances that can be taken offline during update.
                          type: number
                      type: object
                    type: array
                  description:
                    description: |-
                      (String). The resource description.
                      The resource description.
                    type: string
                  instanceTemplate:
                    description: |-
                      [Block]. Template used to create compute instances in this Kubernetes node group.
                      Template used to create compute instances in this Kubernetes node group.
                    items:
                      properties:
                        bootDisk:
                          description: |-
                            [Block]. The specifications for boot disks that will be attached to the instance.
                            The specifications for boot disks that will be attached to the instance.
                          items:
                            properties:
                              size:
                                description: |-
                                  (Number). The number of instances in the node group.
                                  The size of the disk in GB. Allowed minimal size: 64 GB.
                                type: number
                              type:
                                description: |-
                                  (String). Type of container runtime. Values: docker, containerd.
                                  The disk type.
                                type: string
                            type: object
                          type: array
                        containerNetwork:
                          description: |-
                            [Block]. Container network configuration.
                            Container network configuration.
                          items:
                            properties:
                              podMtu:
                                description: |-
                                  (Number). MTU for pods.
                                  MTU for pods.
                                type: number
                            type: object
                          type: array
                        containerRuntime:
                          description: |-
                            [Block]. Container runtime configuration.
                            Container runtime configuration.
                          items:
                            properties:
                              type:
                                description: |-
                                  (String). Type of container runtime. Values: docker, containerd.
                                  Type of container runtime. Values: `docker`, `containerd`.
                                type: string
                            type: object
                          type: array
                        gpuSettings:
                          description: |-
                            [Block]. GPU settings.
                            GPU settings.
                          items:
                            properties:
                              gpuClusterId:
                                description: |-
                                  (String). GPU cluster id.
                                  GPU cluster id.
                                type: string
                              gpuEnvironment:
                                description: |-
                                  (String). GPU environment. Values: runc, runc_drivers_cuda.
                                  GPU environment. Values: `runc`, `runc_drivers_cuda`.
                                type: string
                            type: object
                          type: array
                        index:
                          default: default
                          description: This is an injected field with a default value
                            for being able to merge items of the parent object list.
                          type: string
                        labels:
                          additionalProperties:
                            type: string
                          description: |-
                            (Map Of String). A set of key/value label pairs which assigned to resource.
                            Labels that will be assigned to compute nodes (instances), created by the Node Group.
                          type: object
                          x-kubernetes-map-type: granular
                        metadata:
                          additionalProperties:
                            type: string
                          description: |-
                            data won't be provided into instances. It reserved for internal activity in kubernetes_node_group resource.
                            The set of metadata `key:value` pairs assigned to this instance template. This includes custom metadata and predefined keys. **Note**: key `user-data` won't be provided into instances. It reserved for internal activity in `kubernetes_node_group` resource.
                          type: object
                          x-kubernetes-map-type: granular
                        name:
                          description: |-
                            (String). The resource name.
                            Name template of the instance. In order to be unique it must contain at least one of instance unique placeholders:
                            * `{instance.short_id}`
                            * `{instance.index}`
                            * combination of `{instance.zone_id}` and `{instance.index_in_zone}`

                            Example: `my-instance-{instance.index}`.

                            If not set, default is used: `{instance_group.id}-{instance.short_id}`. It may also contain another placeholders, see [Compute Instance group metadata doc](https://yandex.cloud/docs/compute/instancegroup/api-ref/grpc/InstanceGroup) for full list.
                          type: string
                        nat:
                          description: |-
                            (Bool). A public address that can be used to access the internet over NAT.
                            Enables NAT for node group compute instances.
                          type: boolean
                        networkAccelerationType:
                          description: |-
                            (String). Type of network acceleration. Values: standard, software_accelerated.
                            Type of network acceleration. Values: `standard`, `software_accelerated`.
                          type: string
                        networkInterface:
                          description: |-
                            [Block]. An array with the network interfaces that will be attached to the instance.
                            An array with the network interfaces that will be attached to the instance.
                          items:
                            properties:
                              ipv4:
                                description: |-
                                  (Bool). Allocate an IPv4 address for the interface. The default value is true.
                                  Allocate an IPv4 address for the interface. The default value is `true`.
                                type: boolean
                              ipv4DnsRecords:
                                description: |-
                                  [Block]. List of configurations for creating ipv4 DNS records.
                                  List of configurations for creating ipv4 DNS records.
                                items:
                                  properties:
                                    dnsZoneId:
                                      description: |-
                                        (String). DNS zone ID (if not set, private zone is used).
                                        DNS zone ID (if not set, private zone is used).
                                      type: string
                                    fqdn:
                                      description: |-
                                        (String). DNS record FQDN.
                                        DNS record FQDN.
                                      type: string
                                    ptr:
                                      description: |-
                                        (Bool). When set to true, also create a PTR DNS record.
                                        When set to `true`, also create a PTR DNS record.
                                      type: boolean
                                    ttl:
                                      description: |-
                                        (Number). DNS record TTL (in seconds).
                                        DNS record TTL (in seconds).
                                      type: number
                                  type: object
                                type: array
                              ipv6:
                                description: |-
                                  (Bool). If true, allocate an IPv6 address for the interface. The address will be automatically assigned from the specified subnet.
                                  If true, allocate an IPv6 address for the interface. The address will be automatically assigned from the specified subnet.
                                type: boolean
                              ipv6DnsRecords:
                                description: |-
                                  [Block]. List of configurations for creating ipv6 DNS records.
                                  List of configurations for creating ipv6 DNS records.
                                items:
                                  properties:
                                    dnsZoneId:
                                      description: |-
                                        (String). DNS zone ID (if not set, private zone is used).
                                        DNS zone ID (if not set, private zone is used).
                                      type: string
                                    fqdn:
                                      description: |-
                                        (String). DNS record FQDN.
                                        DNS record FQDN.
                                      type: string
                                    ptr:
                                      description: |-
                                        (Bool). When set to true, also create a PTR DNS record.
                                        When set to `true`, also create a PTR DNS record.
                                      type: boolean
                                    ttl:
                                      description: |-
                                        (Number). DNS record TTL (in seconds).
                                        DNS record TTL (in seconds).
                                      type: number
                                  type: object
                                type: array
                              nat:
                                description: |-
                                  (Bool). A public address that can be used to access the internet over NAT.
                                  A public address that can be used to access the internet over NAT.
                                type: boolean
                              securityGroupIds:
                                description: |-
                                  (Set Of String). Security group IDs for network interface.
                                  Security group IDs for network interface.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              securityGroupIdsRefs:
                                description: References to SecurityGroup in vpc to
                                  populate securityGroupIds.
                                items:
                                  description: A NamespacedReference to a named object.
                                  properties:
                                    name:
                                      description: Name of the referenced object.
                                      type: string
                                    namespace:
                                      description: Namespace of the referenced object
                                      type: string
                                    policy:
                                      description: Policies for referencing.
                                      properties:
                                        resolution:
                                          default: Required
                                          description: |-
                                            Resolution specifies whether resolution of this reference is required.
                                            The default is 'Required', which means the reconcile will fail if the
                                            reference cannot be resolved. 'Optional' means this reference will be
                                            a no-op if it cannot be resolved.
                                          enum:
                                          - Required
                                          - Optional
                                          type: string
                                        resolve:
                                          description: |-
                                            Resolve specifies when this reference should be resolved. The default
                                            is 'IfNotPresent', which will attempt to resolve the reference only when
                                            the corresponding field is not present. Use 'Always' to resolve the
                                            reference on every reconcile.
                                          enum:
                                          - Always
                                          - IfNotPresent
                                          type: string
                                      type: object
                                  required:
                                  - name
                                  type: object
                                type: array
                              securityGroupIdsSelector:
                                description: Selector for a list of SecurityGroup
                                  in vpc to populate securityGroupIds.
                                properties:
                                  matchControllerRef:
                                    description: |-
                                      MatchControllerRef ensures an object with the same controller reference
                                      as the selecting object is selected.
                                    type: boolean
                                  matchLabels:
                                    additionalProperties:
                                      type: string
                                    description: MatchLabels ensures an object with
                                      matching labels is selected.
                                    type: object
                                  namespace:
                                    description: Namespace for the selector
                                    type: string
                                  policy:
                                    description: Policies for selection.
                                    properties:
                                      resolution:
                                        default: Required
                                        description: |-
                                          Resolution specifies whether resolution of this reference is required.
                                          The default is 'Required', which means the reconcile will fail if the
                                          reference cannot be resolved. 'Optional' means this reference will be
                                          a no-op if it cannot be resolved.
                                        enum:
                                        - Required
                                        - Optional
                                        type: string
                                      resolve:
                                        description: |-
                                          Resolve specifies when this reference should be resolved. The default
                                          is 'IfNotPresent', which will attempt to resolve the reference only when
                                          the corresponding field is not present. Use 'Always' to resolve the
                                          reference on every reconcile.
                                        enum:
                                        - Always
                                        - IfNotPresent
                                        type: string
                                    type: object
                                type: object
                              subnetIds:
                                description: |-
                                  (Set Of String). The IDs of the subnets.
                                  The IDs of the subnets.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              subnetIdsRefs:
                                description: References to Subnet in vpc to populate
                                  subnetIds.
                                items:
                                  description: A NamespacedReference to a named object.
                                  properties:
                                    name:
                                      description: Name of the referenced object.
                                      type: string
                                    namespace:
                                      description: Namespace of the referenced object
                                      type: string
                                    policy:
                                      description: Policies for referencing.
                                      properties:
                                        resolution:
                                          default: Required
                                          description: |-
                                            Resolution specifies whether resolution of this reference is required.
                                            The default is 'Required', which means the reconcile will fail if the
                                            reference cannot be resolved. 'Optional' means this reference will be
                                            a no-op if it cannot be resolved.
                                          enum:
                                          - Required
                                          - Optional
                                          type: string
                                        resolve:
                                          description: |-
                                            Resolve specifies when this reference should be resolved. The default
                                            is 'IfNotPresent', which will attempt to resolve the reference only when
                                            the corresponding field is not present. Use 'Always' to resolve the
                                            reference on every reconcile.
                                          enum:
                                          - Always
                                          - IfNotPresent
                                          type: string
                                      type: object
                                  required:
                                  - name
                                  type: object
                                type: array
                              subnetIdsSelector:
                                description: Selector for a list of Subnet in vpc
                                  to populate subnetIds.
                                properties:
                                  matchControllerRef:
                                    description: |-
                                      MatchControllerRef ensures an object with the same controller reference
                                      as the selecting object is selected.
                                    type: boolean
                                  matchLabels:
                                    additionalProperties:
                                      type: string
                                    description: MatchLabels ensures an object with
                                      matching labels is selected.
                                    type: object
                                  namespace:
                                    description: Namespace for the selector
                                    type: string
                                  policy:
                                    description: Policies for selection.
                                    properties:
                                      resolution:
                                        default: Required
                                        description: |-
                                          Resolution specifies whether resolution of this reference is required.
                                          The default is 'Required', which means the reconcile will fail if the
                                          reference cannot be resolved. 'Optional' means this reference will be
                                          a no-op if it cannot be resolved.
                                        enum:
                                        - Required
                                        - Optional
                                        type: string
                                      resolve:
                                        description: |-
                                          Resolve specifies when this reference should be resolved. The default
                                          is 'IfNotPresent', which will attempt to resolve the reference only when
                                          the corresponding field is not present. Use 'Always' to resolve the
                                          reference on every reconcile.
                                        enum:
                                        - Always
                                        - IfNotPresent
                                        type: string
                                    type: object
                                type: object
                            type: object
                          type: array
                        placementPolicy:
                          description: |-
                            [Block]. The placement policy configuration.
                            The placement policy configuration.
                          items:
                            properties:
                              placementGroupId:
                                description: |-
                                  (String). Specifies the id of the Placement Group to assign to the instances.
                                  Specifies the id of the Placement Group to assign to the instances.
                                type: string
                            type: object
                          type: array
                        platformId:
                          description: |-
                            (String). The ID of the hardware platform configuration for the node group compute instances.
                            The ID of the hardware platform configuration for the node group compute instances.
                          type: string
                        reservedInstancePoolId:
                          description: |-
                            (String). ID of the reserved instance pool.
                            ID of the reserved instance pool.
                          type: string
                        resources:
                          description: |-
                            [Block]. Instance resource configuration.
                            Instance resource configuration.
                          items:
                            properties:
                              coreFraction:
                                description: |-
                                  (Number). Baseline core performance as a percent.
                                  Baseline core performance as a percent.
                                type: number
                              cores:
                                description: |-
                                  (Number). Number of CPU cores allocated to the instance.
                                  Number of CPU cores allocated to the instance.
                                type: number
                              gpus:
                                description: |-
                                  (Number). Number of GPU cores allocated to the instance.
                                  Number of GPU cores allocated to the instance.
                                type: number
                              memory:
                                description: |-
                                  (Number). The memory size allocated to the instance.
                                  The memory size allocated to the instance.
                                type: number
                            type: object
                          type: array
                        schedulingPolicy:
                          description: |-
                            [Block]. The scheduling policy for the instances in node group.
                            The scheduling policy for the instances in node group.
                          items:
                            properties:
                              preemptible:
                                description: |-
                                  (Bool). Specifies if the instance is preemptible. Defaults to false.
                                  Specifies if the instance is preemptible. Defaults to `false`.
                                type: boolean
                            type: object
                          type: array
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - index
                    x-kubernetes-list-type: map
                  labels:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map Of String). A set of key/value label pairs which assigned to resource.
                      A set of key/value label pairs which assigned to resource.
                    type: object
                    x-kubernetes-map-type: granular
                  maintenancePolicy:
                    description: |-
                      >1.30) should be performed manually.
                      Maintenance policy for this Kubernetes node group. If policy is omitted, automatic revision upgrades are enabled and could happen at any time. Revision upgrades are performed only within the same minor version, e.g. `1.29`. Minor version upgrades (e.g. `1.29`->`1.30`) should be performed manually.
                    items:
                      properties:
                        autoRepair:
                          description: |-
                            (Bool). Flag that specifies if node group can be repaired automatically. When omitted, default value is true.
                            Flag that specifies if node group can be repaired automatically. When omitted, default value is `true`.
                          type: boolean
                        autoUpgrade:
                          description: |-
                            (Bool). Flag specifies if node group can be upgraded automatically. When omitted, default value is true.
                            Flag specifies if node group can be upgraded automatically. When omitted, default value is `true`.
                          type: boolean
                        maintenanceWindow:
                          description: |-
                            [Block]. Set of day intervals, when maintenance is allowed for this node group. When omitted, it defaults to any time.
                            Set of day intervals, when maintenance is allowed for this node group. When omitted, it defaults to any time.

                            To specify time of day interval, for all days, one element should be provided, with two fields set, `start_time` and `duration`.

                            To allow maintenance only on specific days of week, please provide list of elements, with all fields set. Only one time interval is allowed for each day of week. Please see `my_node_group` config example.
                          items:
                            properties:
                              day:
                                description: |-
                                  (String). Day of week, on which maintenance is allowed.
                                  Day of week, on which maintenance is allowed.
                                type: string
                              duration:
                                description: |-
                                  (String). Duration of maintenance from start_time.
                                  Duration of maintenance from start_time.
                                type: string
                              startTime:
                                description: |-
                                  and duration.
                                  Start time of maintenance in day.
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  name:
                    description: |-
                      (String). The resource name.
                      The resource name.
                    type: string
                  nodeLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map Of String). A set of key/value label pairs, that are assigned to all the nodes of this Kubernetes node group.
                      A set of key/value label pairs, that are assigned to all the nodes of this Kubernetes node group.
                    type: object
                    x-kubernetes-map-type: granular
                  nodeTaints:
                    description: |-
                      (List Of String). A list of Kubernetes taints, that are applied to all the nodes of this Kubernetes node group.
                      A list of Kubernetes taints, that are applied to all the nodes of this Kubernetes node group.
                    items:
                      type: string
                    type: array
                  scalePolicy:
                    description: |-
                      [Block]. Scale policy of the node group.
                      Scale policy of the node group.
                    items:
                      properties:
                        autoScale:
                          description: |-
                            [Block]. Scale policy for an autoscaled node group.
                            Scale policy for an autoscaled node group.
                          items:
                            properties:
                              initial:
                                description: |-
                                  (Number). Initial number of instances in the node group.
                                  Initial number of instances in the node group.
                                type: number
                              max:
                                description: |-
                                  (Number). Maximum number of instances in the node group.
                                  Maximum number of instances in the node group.
                                type: number
                              min:
                                description: |-
                                  (Number). Minimum number of instances in the node group.
                                  Minimum number of instances in the node group.
                                type: number
                            type: object
                          type: array
                        fixedScale:
                          description: |-
                            [Block]. Scale policy for a fixed scale node group.
                            Scale policy for a fixed scale node group.
                          items:
                            properties:
                              size:
                                description: |-
                                  (Number). The number of instances in the node group.
                                  The number of instances in the node group.
                                type: number
                            type: object
                          type: array
                      type: object
                    type: array
                  variables:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map Of String). Variables for templating as key/value pairs.
                      Variables for templating as key/value pairs.
                    type: object
                    x-kubernetes-map-type: granular
                  version:
                    description: |-
                      (String). Version of Kubernetes that will be used for Kubernetes node group.
                      Version of Kubernetes that will be used for Kubernetes node group.
                    type: string
                  workloadIdentityFederation:
                    description: |-
                      [Block]. Workload Identity Federation configuration.
                      Workload Identity Federation configuration.
                    items:
                      properties:
                        enabled:
                          description: |-
                            (Bool). Identifies whether Workload Identity Federation is enabled.
                            Identifies whether Workload Identity Federation is enabled.
                          type: boolean
                      type: object
                    type: array
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  allocationPolicy:
                    description: |-
                      [Block]. This argument specify subnets (zones), that will be used by node group compute instances.
                      This argument specify subnets (zones), that will be used by node group compute instances.
                    items:
                      properties:
                        location:
                          description: |-
                            [Block]. Repeated field, that specify subnets (zones), that will be used by node group compute instances. Subnet specified by subnet_id should be allocated in zone specified by 'zone' argument.
                            Repeated field, that specify subnets (zones), that will be used by node group compute instances. Subnet specified by `subnet_id` should be allocated in zone specified by 'zone' argument.
                          items:
                            properties:
                              subnetId:
                                description: |-
                                  (String). ID of the subnet, that will be used by one compute instance in node group.
                                  ID of the subnet, that will be used by one compute instance in node group.
                                type: string
                              subnetIdRef:
                                description: Reference to a Subnet in vpc to populate
                                  subnetId.
                                properties:
                                  name:
                                    description: Name of the referenced object.
                                    type: string
                                  namespace:
                                    description: Namespace of the referenced object
                                    type: string
                                  policy:
                                    description: Policies for referencing.
                                    properties:
                                      resolution:
                                        default: Required
                                        description: |-
                                          Resolution specifies whether resolution of this reference is required.
                                          The default is 'Required', which means the reconcile will fail if the
                                          reference cannot be resolved. 'Optional' means this reference will be
                                          a no-op if it cannot be resolved.
                                        enum:
                                        - Required
                                        - Optional
                                        type: string
                                      resolve:
                                        description: |-
                                          Resolve specifies when this reference should be resolved. The default
                                          is 'IfNotPresent', which will attempt to resolve the reference only when
                                          the corresponding field is not present. Use 'Always' to resolve the
                                          reference on every reconcile.
                                        enum:
                                        - Always
                                        - IfNotPresent
                                        type: string
                                    type: object
                                required:
                                - name
                                type: object
                              subnetIdSelector:
                                description: Selector for a Subnet in vpc to populate
                                  subnetId.
                                properties:
                                  matchControllerRef:
                                    description: |-
                                      MatchControllerRef ensures an object with the same controller reference
                                      as the selecting object is selected.
                                    type: boolean
                                  matchLabels:
                                    additionalProperties:
                                      type: string
                                    description: MatchLabels ensures an object with
                                      matching labels is selected.
                                    type: object
                                  namespace:
                                    description: Namespace for the selector
                                    type: string
                                  policy:
                                    description: Policies for selection.
                                    properties:
                                      resolution:
                                        default: Required
                                        description: |-
                                          Resolution specifies whether resolution of this reference is required.
                                          The default is 'Required', which means the reconcile will fail if the
                                          reference cannot be resolved. 'Optional' means this reference will be
                                          a no-op if it cannot be resolved.
                                        enum:
                                        - Required
                                        - Optional
                                        type: string
                                      resolve:
                                        description: |-
                                          Resolve specifies when this reference should be resolved. The default
                                          is 'IfNotPresent', which will attempt to resolve the reference only when
                                          the corresponding field is not present. Use 'Always' to resolve the
                                          reference on every reconcile.
                                        enum:
                                        - Always
                                        - IfNotPresent
                                        type: string
                                    type: object
                                type: object
                              zone:
                                description: |-
                                  (String). ID of the availability zone where for one compute instance in node group.
                                  ID of the availability zone where for one compute instance in node group.
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  allowedUnsafeSysctls:
                    description: |-
                      (List Of String). A list of allowed unsafe sysctl parameters for this node group. For more details see documentation.
                      A list of allowed unsafe `sysctl` parameters for this node group. For more details see [documentation](https://kubernetes.io/docs/tasks/administer-cluster/sysctl-cluster).
                    items:
                      type: string
                    type: array
                  clusterId:
                    description: |-
                      (String). The ID of the Kubernetes cluster that this node group belongs to.
                      The ID of the Kubernetes cluster that this node group belongs to.
                    type: string
                  clusterIdRef:
                    description: Reference to a Cluster to populate clusterId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  clusterIdSelector:
                    description: Selector for a Cluster to populate clusterId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  deployPolicy:
                    description: |-
                      [Block]. Deploy policy of the node group.
                      Deploy policy of the node group.
                    items:
                      properties:
                        maxExpansion:
                          description: |-
                            (Number). The maximum number of instances that can be temporarily allocated above the group's target size during the update.
                            The maximum number of instances that can be temporarily allocated above the group's target size during the update.
                          type: number
                        maxUnavailable:
                          description: |-
                            (Number). The maximum number of running instances that can be taken offline during update.
                            The maximum number of running instances that can be taken offline during update.
                          type: number
                      type: object
                    type: array
                  description:
                    description: |-
                      (String). The resource description.
                      The resource description.
                    type: string
                  instanceTemplate:
                    description: |-
                      [Block]. Template used to create compute instances in this Kubernetes node group.
                      Template used to create compute instances in this Kubernetes node group.
                    items:
                      properties:
                        bootDisk:
                          description: |-
                            [Block]. The specifications for boot disks that will be attached to the instance.
                            The specifications for boot disks that will be attached to the instance.
                          items:
                            properties:
                              size:
                                description: |-
                                  (Number). The number of instances in the node group.
                                  The size of the disk in GB. Allowed minimal size: 64 GB.
                                type: number
                              type:
                                description: |-
                                  (String). Type of container runtime. Values: docker, containerd.
                                  The disk type.
                                type: string
                            type: object
                          type: array
                        containerNetwork:
                          description: |-
                            [Block]. Container network configuration.
                            Container network configuration.
                          items:
                            properties:
                              podMtu:
                                description: |-
                                  (Number). MTU for pods.
                                  MTU for pods.
                                type: number
                            type: object
                          type: array
                        containerRuntime:
                          description: |-
                            [Block]. Container runtime configuration.
                            Container runtime configuration.
                          items:
                            properties:
                              type:
                                description: |-
                                  (String). Type of container runtime. Values: docker, containerd.
                                  Type of container runtime. Values: `docker`, `containerd`.
                                type: string
                            type: object
                          type: array
                        gpuSettings:
                          description: |-
                            [Block]. GPU settings.
                            GPU settings.
                          items:
                            properties:
                              gpuClusterId:
                                description: |-
                                  (String). GPU cluster id.
                                  GPU cluster id.
                                type: string
                              gpuEnvironment:
                                description: |-
                                  (String). GPU environment. Values: runc, runc_drivers_cuda.
                                  GPU environment. Values: `runc`, `runc_drivers_cuda`.
                                type: string
                            type: object
                          type: array
                        index:
                          default: default
                          description: This is an injected field with a default value
                            for being able to merge items of the parent object list.
                          type: string
                        labels:
                          additionalProperties:
                            type: string
                          description: |-
                            (Map Of String). A set of key/value label pairs which assigned to resource.
                            Labels that will be assigned to compute nodes (instances), created by the Node Group.
                          type: object
                          x-kubernetes-map-type: granular
                        metadata:
                          additionalProperties:
                            type: string
                          description: |-
                            data won't be provided into instances. It reserved for internal activity in kubernetes_node_group resource.
                            The set of metadata `key:value` pairs assigned to this instance template. This includes custom metadata and predefined keys. **Note**: key `user-data` won't be provided into instances. It reserved for internal activity in `kubernetes_node_group` resource.
                          type: object
                          x-kubernetes-map-type: granular
                        name:
                          description: |-
                            (String). The resource name.
                            Name template of the instance. In order to be unique it must contain at least one of instance unique placeholders:
                            * `{instance.short_id}`
                            * `{instance.index}`
                            * combination of `{instance.zone_id}` and `{instance.index_in_zone}`

                            Example: `my-instance-{instance.index}`.

                            If not set, default is used: `{instance_group.id}-{instance.short_id}`. It may also contain another placeholders, see [Compute Instance group metadata doc](https://yandex.cloud/docs/compute/instancegroup/api-ref/grpc/InstanceGroup) for full list.
                          type: string
                        nat:
                          description: |-
                            (Bool). A public address that can be used to access the internet over NAT.
                            Enables NAT for node group compute instances.
                          type: boolean
                        networkAccelerationType:
                          description: |-
                            (String). Type of network acceleration. Values: standard, software_accelerated.
                            Type of network acceleration. Values: `standard`, `software_accelerated`.
                          type: string
                        networkInterface:
                          description: |-
                            [Block]. An array with the network interfaces that will be attached to the instance.
                            An array with the network interfaces that will be attached to the instance.
                          items:
                            properties:
                              ipv4:
                                description: |-
                                  (Bool). Allocate an IPv4 address for the interface. The default value is true.
                                  Allocate an IPv4 address for the interface. The default value is `true`.
                                type: boolean
                              ipv4DnsRecords:
                                description: |-
                                  [Block]. List of configurations for creating ipv4 DNS records.
                                  List of configurations for creating ipv4 DNS records.
                                items:
                                  properties:
                                    dnsZoneId:
                                      description: |-
                                        (String). DNS zone ID (if not set, private zone is used).
                                        DNS zone ID (if not set, private zone is used).
                                      type: string
                                    fqdn:
                                      description: |-
                                        (String). DNS record FQDN.
                                        DNS record FQDN.
                                      type: string
                                    ptr:
                                      description: |-
                                        (Bool). When set to true, also create a PTR DNS record.
                                        When set to `true`, also create a PTR DNS record.
                                      type: boolean
                                    ttl:
                                      description: |-
                                        (Number). DNS record TTL (in seconds).
                                        DNS record TTL (in seconds).
                                      type: number
                                  type: object
                                type: array
                              ipv6:
                                description: |-
                                  (Bool). If true, allocate an IPv6 address for the interface. The address will be automatically assigned from the specified subnet.
                                  If true, allocate an IPv6 address for the interface. The address will be automatically assigned from the specified subnet.
                                type: boolean
                              ipv6DnsRecords:
                                description: |-
                                  [Block]. List of configurations for creating ipv6 DNS records.
                                  List of configurations for creating ipv6 DNS records.
                                items:
                                  properties:
                                    dnsZoneId:
                                      description: |-
                                        (String). DNS zone ID (if not set, private zone is used).
                                        DNS zone ID (if not set, private zone is used).
                                      type: string
                                    fqdn:
                                      description: |-
                                        (String). DNS record FQDN.
                                        DNS record FQDN.
                                      type: string
                                    ptr:
                                      description: |-
                                        (Bool). When set to true, also create a PTR DNS record.
                                        When set to `true`, also create a PTR DNS record.
                                      type: boolean
                                    ttl:
                                      description: |-
                                        (Number). DNS record TTL (in seconds).
                                        DNS record TTL (in seconds).
                                      type: number
                                  type: object
                                type: array
                              nat:
                                description: |-
                                  (Bool). A public address that can be used to access the internet over NAT.
                                  A public address that can be used to access the internet over NAT.
                                type: boolean
                              securityGroupIds:
                                description: |-
                                  (Set Of String). Security group IDs for network interface.
                                  Security group IDs for network interface.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              securityGroupIdsRefs:
                                description: References to SecurityGroup in vpc to
                                  populate securityGroupIds.
                                items:
                                  description: A NamespacedReference to a named object.
                                  properties:
                                    name:
                                      description: Name of the referenced object.
                                      type: string
                                    namespace:
                                      description: Namespace of the referenced object
                                      type: string
                                    policy:
                                      description: Policies for referencing.
                                      properties:
                                        resolution:
                                          default: Required
                                          description: |-
                                            Resolution specifies whether resolution of this reference is required.
                                            The default is 'Required', which means the reconcile will fail if the
                                            reference cannot be resolved. 'Optional' means this reference will be
                                            a no-op if it cannot be resolved.
                                          enum:
                                          - Required
                                          - Optional
                                          type: string
                                        resolve:
                                          description: |-
                                            Resolve specifies when this reference should be resolved. The default
                                            is 'IfNotPresent', which will attempt to resolve the reference only when
                                            the corresponding field is not present. Use 'Always' to resolve the
                                            reference on every reconcile.
                                          enum:
                                          - Always
                                          - IfNotPresent
                                          type: string
                                      type: object
                                  required:
                                  - name
                                  type: object
                                type: array
                              securityGroupIdsSelector:
                                description: Selector for a list of SecurityGroup
                                  in vpc to populate securityGroupIds.
                                properties:
                                  matchControllerRef:
                                    description: |-
                                      MatchControllerRef ensures an object with the same controller reference
                                      as the selecting object is selected.
                                    type: boolean
                                  matchLabels:
                                    additionalProperties:
                                      type: string
                                    description: MatchLabels ensures an object with
                                      matching labels is selected.
                                    type: object
                                  namespace:
                                    description: Namespace for the selector
                                    type: string
                                  policy:
                                    description: Policies for selection.
                                    properties:
                                      resolution:
                                        default: Required
                                        description: |-
                                          Resolution specifies whether resolution of this reference is required.
                                          The default is 'Required', which means the reconcile will fail if the
                                          reference cannot be resolved. 'Optional' means this reference will be
                                          a no-op if it cannot be resolved.
                                        enum:
                                        - Required
                                        - Optional
                                        type: string
                                      resolve:
                                        description: |-
                                          Resolve specifies when this reference should be resolved. The default
                                          is 'IfNotPresent', which will attempt to resolve the reference only when
                                          the corresponding field is not present. Use 'Always' to resolve the
                                          reference on every reconcile.
                                        enum:
                                        - Always
                                        - IfNotPresent
                                        type: string
                                    type: object
                                type: object
                              subnetIds:
                                description: |-
                                  (Set Of String). The IDs of the subnets.
                                  The IDs of the subnets.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              subnetIdsRefs:
                                description: References to Subnet in vpc to populate
                                  subnetIds.
                                items:
                                  description: A NamespacedReference to a named object.
                                  properties:
                                    name:
                                      description: Name of the referenced object.
                                      type: string
                                    namespace:
                                      description: Namespace of the referenced object
                                      type: string
                                    policy:
                                      description: Policies for referencing.
                                      properties:
                                        resolution:
                                          default: Required
                                          description: |-
                                            Resolution specifies whether resolution of this reference is required.
                                            The default is 'Required', which means the reconcile will fail if the
                                            reference cannot be resolved. 'Optional' means this reference will be
                                            a no-op if it cannot be resolved.
                                          enum:
                                          - Required
                                          - Optional
                                          type: string
                                        resolve:
                                          description: |-
                                            Resolve specifies when this reference should be resolved. The default
                                            is 'IfNotPresent', which will attempt to resolve the reference only when
                                            the corresponding field is not present. Use 'Always' to resolve the
                                            reference on every reconcile.
                                          enum:
                                          - Always
                                          - IfNotPresent
                                          type: string
                                      type: object
                                  required:
                                  - name
                                  type: object
                                type: array
                              subnetIdsSelector:
                                description: Selector for a list of Subnet in vpc
                                  to populate subnetIds.
                                properties:
                                  matchControllerRef:
                                    description: |-
                                      MatchControllerRef ensures an object with the same controller reference
                                      as the selecting object is selected.
                                    type: boolean
                                  matchLabels:
                                    additionalProperties:
                                      type: string
                                    description: MatchLabels ensures an object with
                                      matching labels is selected.
                                    type: object
                                  namespace:
                                    description: Namespace for the selector
                                    type: string
                                  policy:
                                    description: Policies for selection.
                                    properties:
                                      resolution:
                                        default: Required
                                        description: |-
                                          Resolution specifies whether resolution of this reference is required.
                                          The default is 'Required', which means the reconcile will fail if the
                                          reference cannot be resolved. 'Optional' means this reference will be
                                          a no-op if it cannot be resolved.
                                        enum:
                                        - Required
                                        - Optional
                                        type: string
                                      resolve:
                                        description: |-
                                          Resolve specifies when this reference should be resolved. The default
                                          is 'IfNotPresent', which will attempt to resolve the reference only when
                                          the corresponding field is not present. Use 'Always' to resolve the
                                          reference on every reconcile.
                                        enum:
                                        - Always
                                        - IfNotPresent
                                        type: string
                                    type: object
                                type: object
                            type: object
                          type: array
                        placementPolicy:
                          description: |-
                            [Block]. The placement policy configuration.
                            The placement policy configuration.
                          items:
                            properties:
                              placementGroupId:
                                description: |-
                                  (String). Specifies the id of the Placement Group to assign to the instances.
                                  Specifies the id of the Placement Group to assign to the instances.
                                type: string
                            type: object
                          type: array
                        platformId:
                          description: |-
                            (String). The ID of the hardware platform configuration for the node group compute instances.
                            The ID of the hardware platform configuration for the node group compute instances.
                          type: string
                        reservedInstancePoolId:
                          description: |-
                            (String). ID of the reserved instance pool.
                            ID of the reserved instance pool.
                          type: string
                        resources:
                          description: |-
                            [Block]. Instance resource configuration.
                            Instance resource configuration.
                          items:
                            properties:
                              coreFraction:
                                description: |-
                                  (Number). Baseline core performance as a percent.
                                  Baseline core performance as a percent.
                                type: number
                              cores:
                                description: |-
                                  (Number). Number of CPU cores allocated to the instance.
                                  Number of CPU cores allocated to the instance.
                                type: number
                              gpus:
                                description: |-
                                  (Number). Number of GPU cores allocated to the instance.
                                  Number of GPU cores allocated to the instance.
                                type: number
                              memory:
                                description: |-
                                  (Number). The memory size allocated to the instance.
                                  The memory size allocated to the instance.
                                type: number
                            type: object
                          type: array
                        schedulingPolicy:
                          description: |-
                            [Block]. The scheduling policy for the instances in node group.
                            The scheduling policy for the instances in node group.
                          items:
                            properties:
                              preemptible:
                                description: |-
                                  (Bool). Specifies if the instance is preemptible. Defaults to false.
                                  Specifies if the instance is preemptible. Defaults to `false`.
                                type: boolean
                            type: object
                          type: array
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - index
                    x-kubernetes-list-type: map
                  labels:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map Of String). A set of key/value label pairs which assigned to resource.
                      A set of key/value label pairs which assigned to resource.
                    type: object
                    x-kubernetes-map-type: granular
                  maintenancePolicy:
                    description: |-
                      >1.30) should be performed manually.
                      Maintenance policy for this Kubernetes node group. If policy is omitted, automatic revision upgrades are enabled and could happen at any time. Revision upgrades are performed only within the same minor version, e.g. `1.29`. Minor version upgrades (e.g. `1.29`->`1.30`) should be performed manually.
                    items:
                      properties:
                        autoRepair:
                          description: |-
                            (Bool). Flag that specifies if node group can be repaired automatically. When omitted, default value is true.
                            Flag that specifies if node group can be repaired automatically. When omitted, default value is `true`.
                          type: boolean
                        autoUpgrade:
                          description: |-
                            (Bool). Flag specifies if node group can be upgraded automatically. When omitted, default value is true.
                            Flag specifies if node group can be upgraded automatically. When omitted, default value is `true`.
                          type: boolean
                        maintenanceWindow:
                          description: |-
                            [Block]. Set of day intervals, when maintenance is allowed for this node group. When omitted, it defaults to any time.
                            Set of day intervals, when maintenance is allowed for this node group. When omitted, it defaults to any time.

                            To specify time of day interval, for all days, one element should be provided, with two fields set, `start_time` and `duration`.

                            To allow maintenance only on specific days of week, please provide list of elements, with all fields set. Only one time interval is allowed for each day of week. Please see `my_node_group` config example.
                          items:
                            properties:
                              day:
                                description: |-
                                  (String). Day of week, on which maintenance is allowed.
                                  Day of week, on which maintenance is allowed.
                                type: string
                              duration:
                                description: |-
                                  (String). Duration of maintenance from start_time.
                                  Duration of maintenance from start_time.
                                type: string
                              startTime:
                                description: |-
                                  and duration.
                                  Start time of maintenance in day.
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  name:
                    description: |-
                      (String). The resource name.
                      The resource name.
                    type: string
                  nodeLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map Of String). A set of key/value label pairs, that are assigned to all the nodes of this Kubernetes node group.
                      A set of key/value label pairs, that are assigned to all the nodes of this Kubernetes node group.
                    type: object
                    x-kubernetes-map-type: granular
                  nodeTaints:
                    description: |-
                      (List Of String). A list of Kubernetes taints, that are applied to all the nodes of this Kubernetes node group.
                      A list of Kubernetes taints, that are applied to all the nodes of this Kubernetes node group.
                    items:
                      type: string
                    type: array
                  scalePolicy:
                    description: |-
                      [Block]. Scale policy of the node group.
                      Scale policy of the node group.
                    items:
                      properties:
                        autoScale:
                          description: |-
                            [Block]. Scale policy for an autoscaled node group.
                            Scale policy for an autoscaled node group.
                          items:
                            properties:
                              initial:
                                description: |-
                                  (Number). Initial number of instances in the node group.
                                  Initial number of instances in the node group.
                                type: number
                              max:
                                description: |-
                                  (Number). Maximum number of instances in the node group.
                                  Maximum number of instances in the node group.
                                type: number
                              min:
                                description: |-
                                  (Number). Minimum number of instances in the node group.
                                  Minimum number of instances in the node group.
                                type: number
                            type: object
                          type: array
                        fixedScale:
                          description: |-
                            [Block]. Scale policy for a fixed scale node group.
                            Scale policy for a fixed scale node group.
                          items:
                            properties:
                              size:
                                description: |-
                                  (Number). The number of instances in the node group.
                                  The number of instances in the node group.
                                type: number
                            type: object
                          type: array
                      type: object
                    type: array
                  variables:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map Of String). Variables for templating as key/value pairs.
                      Variables for templating as key/value pairs.
                    type: object
                    x-kubernetes-map-type: granular
                  version:
                    description: |-
                      (String). Version of Kubernetes that will be used for Kubernetes node group.
                      Version of Kubernetes that will be used for Kubernetes node group.
                    type: string
                  workloadIdentityFederation:
                    description: |-
                      [Block]. Workload Identity Federation configuration.
                      Workload Identity Federation configuration.
                    items:
                      properties:
                        enabled:
                          description: |-
                            (Bool). Identifies whether Workload Identity Federation is enabled.
                            Identifies whether Workload Identity Federation is enabled.
                          type: boolean
                      type: object
                    type: array
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.instanceTemplate is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.instanceTemplate)
                || (has(self.initProvider) && has(self.initProvider.instanceTemplate))'
            - message: spec.forProvider.scalePolicy is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.scalePolicy)
                || (has(self.initProvider) && has(self.initProvider.scalePolicy))'
          status:
            description: NodeGroupStatus defines the observed state of NodeGroup.
            properties:
              atProvider:
                properties:
                  allocationPolicy:
                    description: |-
                      [Block]. This argument specify subnets (zones), that will be used by node group compute instances.
                      This argument specify subnets (zones), that will be used by node group compute instances.
                    items:
                      properties:
                        location:
                          description: |-
                            [Block]. Repeated field, that specify subnets (zones), that will be used by node group compute instances. Subnet specified by subnet_id should be allocated in zone specified by 'zone' argument.
                            Repeated field, that specify subnets (zones), that will be used by node group compute instances. Subnet specified by `subnet_id` should be allocated in zone specified by 'zone' argument.
                          items:
                            properties:
                              subnetId:
                                description: |-
                                  (String). ID of the subnet, that will be used by one compute instance in node group.
                                  ID of the subnet, that will be used by one compute instance in node group.
                                type: string
                              zone:
                                description: |-
                                  (String). ID of the availability zone where for one compute instance in node group.
                                  ID of the availability zone where for one compute instance in node group.
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  allowedUnsafeSysctls:
                    description: |-
                      (List Of String). A list of allowed unsafe sysctl parameters for this node group. For more details see documentation.
                      A list of allowed unsafe `sysctl` parameters for this node group. For more details see [documentation](https://kubernetes.io/docs/tasks/administer-cluster/sysctl-cluster).
                    items:
                      type: string
                    type: array
                  clusterId:
                    description: |-
                      (String). The ID of the Kubernetes cluster that this node group belongs to.
                      The ID of the Kubernetes cluster that this node group belongs to.
                    type: string
                  createdAt:
                    description: |-
                      Only) (String). The creation timestamp of the resource.
                      The creation timestamp of the resource.
                    type: string
                  deployPolicy:
                    description: |-
                      [Block]. Deploy policy of the node group.
                      Deploy policy of the node group.
                    items:
                      properties:
                        maxExpansion:
                          description: |-
                            (Number). The maximum number of instances that can be temporarily allocated above the group's target size during the update.
                            The maximum number of instances that can be temporarily allocated above the group's target size during the update.
                          type: number
                        maxUnavailable:
                          description: |-
                            (Number). The maximum number of running instances that can be taken offline during update.
                            The maximum number of running instances that can be taken offline during update.
                          type: number
                      type: object
                    type: array
                  description:
                    description: |-
                      (String). The resource description.
                      The resource description.
                    type: string
                  id:
                    description: (String).
                    type: string
                  instanceGroupId:
                    description: |-
                      Only) (String). ID of instance group that is used to manage this Kubernetes node group.
                      ID of instance group that is used to manage this Kubernetes node group.
                    type: string
                  instanceTemplate:
                    description: |-
                      [Block]. Template used to create compute instances in this Kubernetes node group.
                      Template used to create compute instances in this Kubernetes node group.
                    items:
                      properties:
                        bootDisk:
                          description: |-
                            [Block]. The specifications for boot disks that will be attached to the instance.
                            The specifications for boot disks that will be attached to the instance.
                          items:
                            properties:
                              size:
                                description: |-
                                  (Number). The number of instances in the node group.
                                  The size of the disk in GB. Allowed minimal size: 64 GB.
                                type: number
                              type:
                                description: |-
                                  (String). Type of container runtime. Values: docker, containerd.
                                  The disk type.
                                type: string
                            type: object
                          type: array
                        containerNetwork:
                          description: |-
                            [Block]. Container network configuration.
                            Container network configuration.
                          items:
                            properties:
                              podMtu:
                                description: |-
                                  (Number). MTU for pods.
                                  MTU for pods.
                                type: number
                            type: object
                          type: array
                        containerRuntime:
                          description: |-
                            [Block]. Container runtime configuration.
                            Container runtime configuration.
                          items:
                            properties:
                              type:
                                description: |-
                                  (String). Type of container runtime. Values: docker, containerd.
                                  Type of container runtime. Values: `docker`, `containerd`.
                                type: string
                            type: object
                          type: array
                        gpuSettings:
                          description: |-
                            [Block]. GPU settings.
                            GPU settings.
                          items:
                            properties:
                              gpuClusterId:
                                description: |-
                                  (String). GPU cluster id.
                                  GPU cluster id.
                                type: string
                              gpuEnvironment:
                                description: |-
                                  (String). GPU environment. Values: runc, runc_drivers_cuda.
                                  GPU environment. Values: `runc`, `runc_drivers_cuda`.
                                type: string
                            type: object
                          type: array
                        index:
                          description: This is an injected field with a default value
                            for being able to merge items of the parent object list.
                          type: string
                        labels:
                          additionalProperties:
                            type: string
                          description: |-
                            (Map Of String). A set of key/value label pairs which assigned to resource.
                            Labels that will be assigned to compute nodes (instances), created by the Node Group.
                          type: object
                          x-kubernetes-map-type: granular
                        metadata:
                          additionalProperties:
                            type: string
                          description: |-
                            data won't be provided into instances. It reserved for internal activity in kubernetes_node_group resource.
                            The set of metadata `key:value` pairs assigned to this instance template. This includes custom metadata and predefined keys. **Note**: key `user-data` won't be provided into instances. It reserved for internal activity in `kubernetes_node_group` resource.
                          type: object
                          x-kubernetes-map-type: granular
                        name:
                          description: |-
                            (String). The resource name.
                            Name template of the instance. In order to be unique it must contain at least one of instance unique placeholders:
                            * `{instance.short_id}`
                            * `{instance.index}`
                            * combination of `{instance.zone_id}` and `{instance.index_in_zone}`

                            Example: `my-instance-{instance.index}`.

                            If not set, default is used: `{instance_group.id}-{instance.short_id}`. It may also contain another placeholders, see [Compute Instance group metadata doc](https://yandex.cloud/docs/compute/instancegroup/api-ref/grpc/InstanceGroup) for full list.
                          type: string
                        nat:
                          description: |-
                            (Bool). A public address that can be used to access the internet over NAT.
                            Enables NAT for node group compute instances.
                          type: boolean
                        networkAccelerationType:
                          description: |-
                            (String). Type of network acceleration. Values: standard, software_accelerated.
                            Type of network acceleration. Values: `standard`, `software_accelerated`.
                          type: string
                        networkInterface:
                          description: |-
                            [Block]. An array with the network interfaces that will be attached to the instance.
                            An array with the network interfaces that will be attached to the instance.
                          items:
                            properties:
                              ipv4:
                                description: |-
                                  (Bool). Allocate an IPv4 address for the interface. The default value is true.
                                  Allocate an IPv4 address for the interface. The default value is `true`.
                                type: boolean
                              ipv4DnsRecords:
                                description: |-
                                  [Block]. List of configurations for creating ipv4 DNS records.
                                  List of configurations for creating ipv4 DNS records.
                                items:
                                  properties:
                                    dnsZoneId:
                                      description: |-
                                        (String). DNS zone ID (if not set, private zone is used).
                                        DNS zone ID (if not set, private zone is used).
                                      type: string
                                    fqdn:
                                      description: |-
                                        (String). DNS record FQDN.
                                        DNS record FQDN.
                                      type: string
                                    ptr:
                                      description: |-
                                        (Bool). When set to true, also create a PTR DNS record.
                                        When set to `true`, also create a PTR DNS record.
                                      type: boolean
                                    ttl:
                                      description: |-
                                        (Number). DNS record TTL (in seconds).
                                        DNS record TTL (in seconds).
                                      type: number
                                  type: object
                                type: array
                              ipv6:
                                description: |-
                                  (Bool). If true, allocate an IPv6 address for the interface. The address will be automatically assigned from the specified subnet.
                                  If true, allocate an IPv6 address for the interface. The address will be automatically assigned from the specified subnet.
                                type: boolean
                              ipv6DnsRecords:
                                description: |-
                                  [Block]. List of configurations for creating ipv6 DNS records.
                                  List of configurations for creating ipv6 DNS records.
                                items:
                                  properties:
                                    dnsZoneId:
                                      description: |-
                                        (String). DNS zone ID (if not set, private zone is used).
                                        DNS zone ID (if not set, private zone is used).
                                      type: string
                                    fqdn:
                                      description: |-
                                        (String). DNS record FQDN.
                                        DNS record FQDN.
                                      type: string
                                    ptr:
                                      description: |-
                                        (Bool). When set to true, also create a PTR DNS record.
                                        When set to `true`, also create a PTR DNS record.
                                      type: boolean
                                    ttl:
                                      description: |-
                                        (Number). DNS record TTL (in seconds).
                                        DNS record TTL (in seconds).
                                      type: number
                                  type: object
                                type: array
                              nat:
                                description: |-
                                  (Bool). A public address that can be used to access the internet over NAT.
                                  A public address that can be used to access the internet over NAT.
                                type: boolean
                              securityGroupIds:
                                description: |-
                                  (Set Of String). Security group IDs for network interface.
                                  Security group IDs for network interface.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                              subnetIds:
                                description: |-
                                  (Set Of String). The IDs of the subnets.
                                  The IDs of the subnets.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: set
                            type: object
                          type: array
                        placementPolicy:
                          description: |-
                            [Block]. The placement policy configuration.
                            The placement policy configuration.
                          items:
                            properties:
                              placementGroupId:
                                description: |-
                                  (String). Specifies the id of the Placement Group to assign to the instances.
                                  Specifies the id of the Placement Group to assign to the instances.
                                type: string
                            type: object
                          type: array
                        platformId:
                          description: |-
                            (String). The ID of the hardware platform configuration for the node group compute instances.
                            The ID of the hardware platform configuration for the node group compute instances.
                          type: string
                        reservedInstancePoolId:
                          description: |-
                            (String). ID of the reserved instance pool.
                            ID of the reserved instance pool.
                          type: string
                        resources:
                          description: |-
                            [Block]. Instance resource configuration.
                            Instance resource configuration.
                          items:
                            properties:
                              coreFraction:
                                description: |-
                                  (Number). Baseline core performance as a percent.
                                  Baseline core performance as a percent.
                                type: number
                              cores:
                                description: |-
                                  (Number). Number of CPU cores allocated to the instance.
                                  Number of CPU cores allocated to the instance.
                                type: number
                              gpus:
                                description: |-
                                  (Number). Number of GPU cores allocated to the instance.
                                  Number of GPU cores allocated to the instance.
                                type: number
                              memory:
                                description: |-
                                  (Number). The memory size allocated to the instance.
                                  The memory size allocated to the instance.
                                type: number
                            type: object
                          type: array
                        schedulingPolicy:
                          description: |-
                            [Block]. The scheduling policy for the instances in node group.
                            The scheduling policy for the instances in node group.
                          items:
                            properties:
                              preemptible:
                                description: |-
                                  (Bool). Specifies if the instance is preemptible. Defaults to false.
                                  Specifies if the instance is preemptible. Defaults to `false`.
                                type: boolean
                            type: object
                          type: array
                      type: object
                    type: array
                  labels:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map Of String). A set of key/value label pairs which assigned to resource.
                      A set of key/value label pairs which assigned to resource.
                    type: object
                    x-kubernetes-map-type: granular
                  maintenancePolicy:
                    description: |-
                      >1.30) should be performed manually.
                      Maintenance policy for this Kubernetes node group. If policy is omitted, automatic revision upgrades are enabled and could happen at any time. Revision upgrades are performed only within the same minor version, e.g. `1.29`. Minor version upgrades (e.g. `1.29`->`1.30`) should be performed manually.
                    items:
                      properties:
                        autoRepair:
                          description: |-
                            (Bool). Flag that specifies if node group can be repaired automatically. When omitted, default value is true.
                            Flag that specifies if node group can be repaired automatically. When omitted, default value is `true`.
                          type: boolean
                        autoUpgrade:
                          description: |-
                            (Bool). Flag specifies if node group can be upgraded automatically. When omitted, default value is true.
                            Flag specifies if node group can be upgraded automatically. When omitted, default value is `true`.
                          type: boolean
                        maintenanceWindow:
                          description: |-
                            [Block]. Set of day intervals, when maintenance is allowed for this node group. When omitted, it defaults to any time.
                            Set of day intervals, when maintenance is allowed for this node group. When omitted, it defaults to any time.

                            To specify time of day interval, for all days, one element should be provided, with two fields set, `start_time` and `duration`.

                            To allow maintenance only on specific days of week, please provide list of elements, with all fields set. Only one time interval is allowed for each day of week. Please see `my_node_group` config example.
                          items:
                            properties:
                              day:
                                description: |-
                                  (String). Day of week, on which maintenance is allowed.
                                  Day of week, on which maintenance is allowed.
                                type: string
                              duration:
                                description: |-
                                  (String). Duration of maintenance from start_time.
                                  Duration of maintenance from start_time.
                                type: string
                              startTime:
                                description: |-
                                  and duration.
                                  Start time of maintenance in day.
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  name:
                    description: |-
                      (String). The resource name.
                      The resource name.
                    type: string
                  nodeLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map Of String). A set of key/value label pairs, that are assigned to all the nodes of this Kubernetes node group.
                      A set of key/value label pairs, that are assigned to all the nodes of this Kubernetes node group.
                    type: object
                    x-kubernetes-map-type: granular
                  nodeTaints:
                    description: |-
                      (List Of String). A list of Kubernetes taints, that are applied to all the nodes of this Kubernetes node group.
                      A list of Kubernetes taints, that are applied to all the nodes of this Kubernetes node group.
                    items:
                      type: string
                    type: array
                  scalePolicy:
                    description: |-
                      [Block]. Scale policy of the node group.
                      Scale policy of the node group.
                    items:
                      properties:
                        autoScale:
                          description: |-
                            [Block]. Scale policy for an autoscaled node group.
                            Scale policy for an autoscaled node group.
                          items:
                            properties:
                              initial:
                                description: |-
                                  (Number). Initial number of instances in the node group.
                                  Initial number of instances in the node group.
                                type: number
                              max:
                                description: |-
                                  (Number). Maximum number of instances in the node group.
                                  Maximum number of instances in the node group.
                                type: number
                              min:
                                description: |-
                                  (Number). Minimum number of instances in the node group.
                                  Minimum number of instances in the node group.
                                type: number
                            type: object
                          type: array
                        fixedScale:
                          description: |-
                            [Block]. Scale policy for a fixed scale node group.
                            Scale policy for a fixed scale node group.
                          items:
                            properties:
                              size:
                                description: |-
                                  (Number). The number of instances in the node group.
                                  The number of instances in the node group.
                                type: number
                            type: object
                          type: array
                      type: object
                    type: array
                  status:
                    description: |-
                      Only) (String). Status of the Kubernetes node group.
                      Status of the Kubernetes node group.
                    type: string
                  variables:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map Of String). Variables for templating as key/value pairs.
                      Variables for templating as key/value pairs.
                    type: object
                    x-kubernetes-map-type: granular
                  version:
                    description: |-
                      (String). Version of Kubernetes that will be used for Kubernetes node group.
                      Version of Kubernetes that will be used for Kubernetes node group.
                    type: string
                  versionInfo:
                    description: |-
                      Only) (List Of Object). Information about Kubernetes node group version.
                      Information about Kubernetes node group version.
                    items:
                      properties:
                        currentVersion:
                          description: .
                          type: string
                        newRevisionAvailable:
                          description: .
                          type: boolean
                        newRevisionSummary:
                          description: .
                          type: string
                        versionDeprecated:
                          description: .
                          type: boolean
                      type: object
                    type: array
                  workloadIdentityFederation:
                    description: |-
                      [Block]. Workload Identity Federation configuration.
                      Workload Identity Federation configuration.
                    items:
                      properties:
                        enabled:
                          description: |-
                            (Bool). Identifies whether Workload Identity Federation is enabled.
                            Identifies whether Workload Identity Federation is enabled.
                          type: boolean
                      type: object
                    type: array
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
